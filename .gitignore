# ── 环境变量（含密钥，严禁提交）──
.env
.env.local
.env.*.local
# 例外：docker-compose/.env 为 dev 演示默认值（无敏感信息），随仓库提交
!docker-compose/.env

# ── Claude Code 本地配置 ──
# settings.local.json 含 API Token / 本地权限，严禁提交
.claude/settings.local.json
# settings.json 仅含团队共享 hooks（无密钥），随仓库提交
!.claude/settings.json
# 技能（skills）默认视为**个人工作流**，不进仓库（如 sync-master：本机 git 同步用）。
# 若某个技能确实要随项目交付，用 `!.claude/skills/<技能名>/` 显式放行 —— 别为此放开整个目录。
# ⚠️ 2026-09-23 实测订正：写 `.claude/skills/`（带尾斜杠 = 忽略**目录本身**）时，
#    git **不会**下探进该目录，下面的 `!` 放行**永远不生效**（git 明确不允许"父目录被排除时再包含其子文件"）。
#    故本行必须写成 `.claude/skills/*`（只忽略目录**内容**，目录本身仍被下探），放行才有效。
.claude/skills/*
# ── 放行：随项目交付的技能（逐个显式放行，不放开整个目录）──
# deploy：nexus 一键部署（阶段交付后的标准动作）。它编码的是**项目特有**的部署知识
#         （builder 四条脚本、端口与分支真源、模型清单、db-patch 只读对账、镜像 rebuild 判据），
#         不是个人工作流，故入库。真源文档见 .claude/skills/deploy/SKILL.md。
# ⚠️ 2026-09-23 起：**脚本本体已搬出本目录**（`deploy.py` → `scripts/py/deploy.py`）——
#    它属生产链路（与 builder / up.sh 同列），放"个人技能目录"里名不副实；
#    skill 目录只留 SKILL.md（Claude Code 要求 skill 在 `.claude/skills/<名>/`，文档不能搬）。
#    故这条放行**仍需保留**（否则 SKILL.md 不入库）。
!.claude/skills/deploy/

# ── Python（`scripts/py/` 下的项目脚本 / `.tmp/` 下的临时探针脚本）──
__pycache__/
*.pyc

# ── Java / Maven ──
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
# flatten-maven-plugin 的构建期产物（各模块目录下，内含已落版的版本号），不提交
.flattened-pom.xml

# ── Node / Vite ──
node_modules/
dist/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── IDE ──
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp

# ── 日志 ──
logs/
*.log

# ── 系统文件 ──
.DS_Store
Thumbs.db
desktop.ini

# ── Docker 本地数据卷 ──
docker-compose/data/

# ── 第三方源码（供 Agent 直接 Read/Grep，免去每次审批）──
# 用途：子代理核查框架行为（如 RestClient 是否增量交付分片）时，需要读 Spring 源码。
#      放在**工作目录内** → 普通 Read/Grep 即可读，零审批、离线可用、不依赖 Docker；
#      放在宿主 ~/.m2 则属工作目录外，会被 blockReadsOutsideWorkingDirectories 拦下。
# 来源：从 Boot 3.3.13 的 BOM 取 spring-framework.version（=6.1.21），
#      下 spring-{core,web,webmvc}-6.1.21-sources.jar 解包而成。
# 体积约 15MB，纯只读参考，不属于任何构建链路 —— 删掉不影响构建，需要时重下即可。
.deps-src/

# ── Agent 临时工作区（脚本 / 检查产物，**不进仓库**）──
# 约定：临时脚本一律放 .tmp/<agent 名>/（如 .tmp/backend-engineer/check_static.py），
#      **不要**写系统临时目录（/tmp、%TEMP%）与仓库外的任何路径 ——
#      blockReadsOutsideWorkingDirectories 会拦下它们，且每次都要人工批准；
#      放仓库内 → Read/Grep/Glob 零审批、离线可用、随手可查。
# 纪律：它**不属于任何生产链路**（up.sh / 镜像构建 / db-patch 迁移都不读它），随时可整个删掉。
#      要留下来的结论请写进 docs/agent-log/ 或 TC-XX，而不是留在 .tmp/ 里。
.tmp/

# ── 测试运行的临时捕获（含登录 token，严禁提交）──
# 用例脚本的输出重定向到这里（如 .result/tmp-result.txt）。它只是"取证现场"：
# 关键行（响应头、meta/done 帧、日志、指纹）应**摘录进 TC-XX 的「实测」栏**，
# 原始捕获本身不入库 —— 它内含真实 JWT，且体积随帧数增长。删掉不影响任何链路。
.result/

# ── 阶段验收夹具（体积大 / 第三方材料，**不入库**）──
# qa/fixtures/rag/公司年报.pdf 是阶段3 的验收夹具（2026-09-22 用户提供），刻意不入库，两条理由：
#   ① 它是一份真实的第三方公开材料（年报），本仓库是**公开**仓库，再分发有来源与版权问题；
#   ② 1.6MB 且只在**宿主侧**被上传时用到，不参与任何构建链路（builder / up.sh / db-patch 都不读它）。
# 复现方式：自备一份中文、含财务数字的 PDF，放到该目录、保持同名即可（判据与期望答案见 TC-03）。
# 注意：**只忽略 PDF** —— qa/fixtures/rag/ 下的说明文件与生成脚本仍照常入库。
qa/fixtures/rag/*.pdf
