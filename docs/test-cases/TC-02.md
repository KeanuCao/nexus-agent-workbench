# TC-02 统一 AI 网关（阶段2）

> 对应任务：[`docs/task/task.3+统一AI网关.md`](../task/task.3+统一AI网关.md) ｜ 设计：[`docs/design/02-统一AI网关.md`](../design/02-统一AI网关.md)
> 接口契约：[`docs/api/README.md`](../api/README.md) **§6**（机器可读版 `docs/api/openapi.yaml`）—— **以它为准，不从实现代码反推**
> 展开依据：设计文档 **§8 验收对照表**（逐条展开）+ §3.2 帧协议 / §3.3 失败形态表 / §4.4 资源释放 /
> §5.4 流的三个出口 / §6.0 容器重建 / §6.5 nginx 链路。
>
> **记录纪律（与 task.3 的「已实现 ≠ 已验收」同一口径）**：
> 「实测」栏**留空即表示未测**，未实测不得填「通过」。失败时把实际观察**原样**记下来（含错误文案与
> 原始响应），不要写「已修复」—— 修复后重跑并记录新结果。
>
> **实测进度（2026-09-20，用户口径）**：**执行了「前端对话页（2.4）」中 `TC-02-2.4-3` 及其之前的用例**
> —— 即 **`TC-02-2.4-1`**（下拉切换 + 打字机）、**`TC-02-2.4-2`**（多轮上下文）、**`TC-02-2.4-3`**（中文不乱码）
> 三条，**均未报告失败**。
> **其余用例一律「回头再执行」**：含 §1~§3 的协议侧脚本用例（`2.1-*` / `2.2-*` / `2.3-*`，**含 `2.1-1`**）、
> §5 回归、§6 契约复核 —— 这些行的「实测」栏**留空**（本文件自己的规矩：留空即未测，
> 不代表通过也不代表失败）。
> ⚠️ 上述三条**逐条观察值未回传**，故记「已执行（未报告失败）」而**不写「通过」**。
>
> **实测进度（2026-09-21 追加，用户口径）**：**又跑了五条并全部报告通过** —— `TC-02-2.4-4`（EOF 第三出口 /
> 生成中途后端被杀，造法 = 用例规定的 `docker compose restart nexus-backend`，容器时间戳亦佐证）、
> `TC-02-2.4-5`（停止生成 + 连续同角色）、`TC-02-2.4-6`（首请求成功 / `Content-Type` 陷阱）、
> `TC-02-2.4-7`（对话页 401 统一处置）、`TC-02-2.4-8`（`/chat` 直达与刷新）。
> 恢复自证已由 AI 侧在 07:36 复核（容器 `healthy` + 直连 `8089` 与走 nginx `8088` 的 `/api/health` 均 200）。
> ✅ **至此 §4「前端对话页」8 条全部报告通过**（`2.4-1`~`2.4-8`，前三条见上一段）。
> ⚠️ **五条都是「报告通过、逐条观察值未回传」**，故各行的「实测」栏一律记「**通过 + 待补**」，
> **不代填、不按预期反推**。五条各自点名要求记下的项：`2.4-4` 判据 ② 的「红字落在哪一条」、
> `2.4-5` 的「观察到哪种停止形态 + 步骤 ⑤ 契约侧 curl 跑没跑」、`2.4-6` 的「步骤 ② 诊断段跑没跑」、
> `2.4-7` / `2.4-8` 的逐条观察值。
> ⇒ 连带结果：「**桌面端页面侧实际走的是哪个出口**」至今**仍无实测值**（即 `2.4-4` 备注最后那条"未实测确认"）。
> ⚠️ **阶段总验收不受影响、仍未成立**：它要的是 `2.1-1 + 2.1-3 + 2.4-1` 三条组合（见 §5 末），
> 上述五条**都不在**这三条里 —— 而 `2.1-1` / `2.1-3` 两条**协议侧**用例**至今没跑**，是仅剩的缺口。
> **§4 走完 ≠ 阶段走完**：本文件 27 条里，界面侧 9 条已走完（含新增的 `2.4-9`），剩下的全在协议侧（§1~§3 的脚本用例）
> 与 §5 回归 / §6 契约复核。
>
> **实测进度（2026-09-21 追加，协议侧开跑）**：**`TC-02-2.1-1`（本地 Qwen）与 `TC-02-2.1-3`（云端 DeepSeek）
> 双双报告通过，且六条/三条判据逐条有原始帧为证**（捕获在 `.result/tmp-result.txt`；见各行「实测」栏）。
> ⇒ **阶段总验收的三条组合（`2.1-1 + 2.1-3 + 2.4-1`）至此全部有实测记录**：前两条本次**直接读到帧**（不是日志反推），
> `2.4-1` 仍记的是 2026-09-20 的「已执行（未报告失败）」—— **总验收是否据此判定成立，请用户拍板**（本文件不代填）。
> ✅ **顺带订正 charset 措辞（已收口）**：判据 ① 原写 `;charset=UTF-8`，**实测不存在**（响应头是裸 `text/event-stream`，与契约 §6.1 一致）。
> 已拍板「**不加**」（SSE 规范的 `charset` 只是为兼容遗留服务端保留的可选参数、事件流恒为 UTF-8），据此订正了
> **本文档判据 ①** + **4 处文档**（`README` §6.3 / 设计 §5.1-2 / `openapi.yaml` 两处）；`2026-09-18` 那份评审留档里的同款说法
> 按「不改历史留档」原样保留。详见 §7 第 4 行。**代码不受影响**（前端判类型用的是 `includes('application/json')`）。
> ⚠️ **仍未跑**：`2.2-*`（4 条）、`2.3-*`（6 条）、§5 回归（4 条）、§6 契约复核（1 条）—— 协议侧只开了个头。
> **另有 1 条云端侧的用户实测（2026-09-20）**：用户在对话页选「云端 DeepSeek」提问「你是谁」，
> **拿到了真实的 DeepSeek 回答**（自称 DeepSeek、提到 1M 上下文与免费额度）—— 这证明「**选云端 → 走云端**」
> 这条链路**是通的**，且密钥确已注入容器。它落在 **`2.4-1` 的第 ⑥ 轮**（界面侧），
> **不等于 `2.1-3`**（协议侧脚本用例，仍未跑）。
> 另有 **3 条由 qa 侧只读探针实测**（`2.3-6` / `R-1` / §7 的三条偏差证据），**非用户验收**，已在对应行注明。
>
> **实测进度（2026-09-21 追加，新增用例）**：本节新增 **`TC-02-2.4-9`**（不配密钥时在对话页选「云端 DeepSeek」，
> 页面必须如实报错），**当天即由用户手工实测、报告通过**，且**防假通过那半也确认了**（屏幕上没有 Qwen 回答、
> 也没有 `qwen2.5:7b` 的 tag）。
> ⚠️ 但这条挂着两件事：判据 ②③④⑤ 的逐条观察值**未回传**；判据 ⑥「落到哪条形态」因**旧容器日志随重建丢失**
> 而**无从实记**（只能从文件痕迹**推断**是「未配置」形态，不算实测）。
> ⛔→✅ **无害性自证（⑨ 步）当时未达成**（跑完留下 2 条 `#TC02-2.4-9-DISABLED` 注释行、生效行数 1 ≠ 跑前的 2），
> **当天已由用户手工收拾干净**：文件现在只剩 **1 行**生效的 `sk-` 真密钥，**无残留、无重复**，容器已吃到，链路 200。
> ✅ 净效果比跑之前**更好**：那个 14 字符的占位值（它一直在**遮蔽**真密钥）被**彻底移除** ⇒ **云端从"坏"回到"可用"**
> （见 §0 前置条件那格的订正；有效性待 `2.1-3` 实测）。
> ⇒ 本文件用例数 **27 条**（界面侧 9 条）；**至此 §4 九条全部有实测记录**，剩下的全在协议侧（§1~§3）+ §5 回归 / §6 契约复核。
> ⚠️ **阶段总验收**要求 `2.1-1 + 2.1-3 + 2.4-1` 三条组合（见 §5 末）：**云端那一半已被上述 UI 实测事实上验证**，
> 但 `2.1-1` 与 `2.1-3` 两条**协议侧**用例仍未跑 —— 严格按本文件的判据，总验收**尚未成立**。
>
> **可执行纪律（硬要求）**：「步骤」栏必须**可被另一个人无损复现**。
> **命令类**：每格是能直接复制粘贴执行的完整命令（WSL 内），含必需的 `cd <目录>` 前缀。
> **操作类**（§4 的浏览器交互）：写成**逐步编号动作**（点哪个按钮、输入什么、每步立刻能看到什么），同样禁止压缩。
> 一律禁止把多步压成一行箭头串 —— 多步就分行。
> 判据：照着这一格走一遍，**不需要任何补充信息、不需要任何自行翻译**。
>
> **脚本化（2026-09-20）**：本文件里凡"要先登录拿 token、再把 token 喂给下一条命令"的步骤，
> 一律落成 `qa/scripts/tc02-*.sh` —— 步骤栏只留**一行**可复制命令 + 一句「这一步在干什么」。
> 两个脚本共用一个**只装机械动作**的过滤器 `qa/scripts/lib/tc02-stamp-lines.py`（逐行打到达时刻 +
> 机械统计），并复用 TC-01 立的公共库 `qa/scripts/lib/tc-common.sh`（路径/端口解析、JSON 取值、
> HTTP 动作、Redis 只读指纹、退出路径的清理脚手架）。
> **判据一律留在本文档**：脚本只做动作、只打印观察，**不打 PASS/FAIL**，退出码只表示「动作做没做成」。
> 两个脚本都自带**幂等清理**（登出 + Ctrl-C/kill 的 trap 兜底）与跑前/跑后 **Redis 键清单指纹**自证。
> 先自检一次再跑（纯本地，不碰环境）：
> `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc02-chat-stream.sh --self-test`
>
> **编号对照（必读，别混）**：本文件的 `TC-02-X.Y-N` 是「**用例序号**」（同小节内第 N 条用例）；
> 而 task.3 原文与 [`design/02`](../design/02-统一AI网关.md) §8 的 `X.Y-N` 是「**验收标准**」。
> 两套编号**不是一回事**（TC-01 已因这件事吃过一次亏），凡是验收标准被拆成多条用例的地方都会错位：
>
> | 验收标准（task.3 / design §8） | 本文档的用例 |
> | --- | --- |
> | 2.1-1 两个实现各自可独立完成对话 | **TC-02-2.1-1**（本地 Qwen）+ **TC-02-2.1-3**（云端 DeepSeek） |
> | 2.1-2 关键流程有 `log.info` 链路日志 | **TC-02-2.1-2** |
> | 2.2-1 传 OLLAMA / DEEPSEEK 分别命中对应实现 | **TC-02-2.2-1** |
> | 2.2-2 未知类型抛业务异常 | **TC-02-2.2-2** |
> | 2.3-1 curl 可见增量分片而非一次性返回 | **TC-02-2.3-1**（直连后端）+ **TC-02-2.3-2**（走 nginx 8088） |
> | 2.3-2 客户端中断时 SSE 资源正确释放 | **TC-02-2.3-3**（curl 侧 Ctrl-C）+ **TC-02-2.4-5**（前端点「停止生成」） |
> | 2.4-1 下拉切换模型，实时打字机输出 | **TC-02-2.4-1** |
> | 2.4-2 多轮上下文保持 | **TC-02-2.4-2** |
> | 2.4-3 中文不乱码 | **TC-02-2.4-3** |
> | 阶段验收（选谁走谁、均能实时打字机） | 2.1-1 + 2.1-3 + 2.4-1 的组合实测 |
> | 0/1 回归（不得破坏既有交付物） | **§5 TC-02-R-1 ~ R-4** |
> | 契约（先落文档） | **§6 TC-02-C-1**（静态复核） |
>
> 引用时请**带 `TC-02-` 前缀**，别只写 `2.3-2`。
>
> **没有对应验收标准的用例**（本文档新增，理由写在各行「备注」里）：`TC-02-2.2-3`（`modelType` 缺省）、
> `TC-02-2.2-4`（请求体校验 `40001`）、`TC-02-2.3-4`（上游不可用**且不降级**）、`TC-02-2.3-5`（池满 `503`）、
> `TC-02-2.3-6`（无 token 的开流前出口）、`TC-02-2.4-4`（EOF 第三出口）、`TC-02-2.4-6`（首请求 `415` 陷阱）、
> `TC-02-2.4-7`（对话页 401 统一处置）、`TC-02-2.4-8`（新增路由的直达与刷新）、
> `TC-02-2.4-9`（不配密钥时页面如实报错 —— 界面侧细化 2.3-4 的页面半，落在决策 **D7** 的防假通过上）。
> 它们都出自**已确认的契约**（§6.1 的三条补充约束、§6.3 的失败形态表、§5 的五个坑）与**已拍板的决策**
> （D7「本轮不做降级链」），不是凭空加戏。

## 0. 前置条件

| 项 | 值 |
|---|---|
| 环境 | 六个容器已起（`nexus-backend` / `nexus-frontend` 均 `healthy`）；`./scripts/check-health.sh` 无 FAIL（基线见 §5 的 R-2） |
| 后端 | `GET /api/health` → 200 + `code=0` + `checks` 三项 UP（**2026-09-20 实测**：`version=0.2.2`、postgres/redis/ollama 全 UP） |
| 模型 | Ollama 容器内有 `qwen2.5:7b`（`curl -s localhost:11434/api/tags`）——**已实测** |
| 端口 | 后端 `BACKEND_PORT=8089`、前端 `FRONTEND_PORT=8088`（唯一真源 `docker-compose/.env`；脚本自动取，**不写死**） |
| **云端密钥** | `docker-compose/.env.local`（内含 `DEEPSEEK_API_KEY`）。**2026-09-20 更新：密钥已配置** —— 证据是用户当天在对话页选「云端 DeepSeek」提问「你是谁」，**拿到了真实的 DeepSeek 回答**（自称 DeepSeek、提到 1M 上下文与免费额度，可确认为真实云端返回）。⇒ 依赖云端的用例（**2.1-3 / 2.2-1 ②**）前置**已满足**。<br>⚠️ **连带影响**：**2.3-4 与 2.4-9 的前置（"容器内没有密钥"）因此都不再成立** —— 要跑 2.3-4 得按该条备注的 ② 路（临时把密钥改成一个明显错误的串 → `up -d --force-recreate` → 跑 → 还原），或把密钥摘掉；**2.4-9 走的就是"把那一行注释掉"那条路**（步骤与还原自证见该条）<br>⛔ **2026-09-21 复核（AI 侧只读探针，**非用户验收**）：上面"密钥已配置"的结论**已失效**。** `.env.local` 现有 **2 行** `DEEPSEEK_API_KEY=`（第 1 行 35 字符、以 `sk-` 开头；第 2 行 14 字符、不以 `sk-` 开头），而 compose 的 `env_file` **后面的行覆盖前面的行**（实测：**容器里当前的值 = 第 2 行的 md5**）⇒ **容器里那个值不是那份 `sk-` 开头的密钥**，云端请求会真的发出去，预期以 `DeepSeek 返回错误状态：status=401` → `20100` 收场（**未实跑验证**，推断依据是容器值形态 + 启动日志 `apiKey已配置=true`）。⇒ 依赖云端的用例（**2.1-3 / 2.2-1 ②**）前置**当前不满足**；**2.4-9 的两种跑法要分清**：按用例步骤 ①~③ 停用（把两行都注释掉）再跑 ⇒ 走「**未配置**」形态；**不做停用、直接在页面发一条云端消息** ⇒ 走「**密钥无效（401）**」形态。两者页面文案相同、后端日志不同 —— 判据 ⑥ 按**实际**那条记。要把云端恢复正常：让那份 `sk-` 开头的密钥成为**最后一行**（或删掉第 2 行），再 `up -d --force-recreate nexus-backend` |
| `nexus-frontend` 镜像 | **2026-09-20 实测**已含 `proxy_buffering off`（`docker exec nexus-frontend grep -n proxy_buffering /etc/nginx/conf.d/default.conf` → `51: proxy_buffering off;`）—— 这是 **2.3-2** 能过的前提 |
| 浏览器 | Chrome / Edge，需要 DevTools 的三处：**Console**（2.4-3 的乱码判据）、**Application → Local Storage**（2.4-7）、**Network**（2.4-4 的可选造法） |
| 日志窗口 | `docker logs -f nexus-backend` 开着（多条判据要看 `[chat]` 日志；脚本自己也会抓一段作佐证） |
| 已知噪音 | `docker compose` 每次都会打 `level=warning msg="The \"model\" variable is not set…"`。它来自**阶段0** `ollama-init` 那段命令里的注释 `${model}`（在 YAML 块标量里没转义），**与阶段2 无关**，别当成故障（`docker-compose.yml:111`） |

### 0.1 无害性总声明（Test Harmlessness —— 先读这一节再看用例）

**跑完本文件之后，被测环境必须与跑之前一致。** 下面把每一处"会留痕的东西"逐项声明；凡是没在这一节里
声明的写操作，**都不允许**在本文件里出现。

| 用例类别 | 会写什么 | 写在哪 / 为什么非写不可 | 清理与自证 |
|---|---|---|---|
| **脚本类**（2.1 / 2.2 / 2.3 的 curl 部分 / 2.4-3 ③） | Redis **1 个**键 `nexus:auth:token:{jti}`（TTL 7200s） | 白名单里必须有本次会话的 jti，否则 `/api/chat/stream` 连开流都开不了（它**不在**白名单接口里）。键由**真登录**产生 | 脚本跑完**登出该 token**（幂等）+ 跑前/跑后 **键清单指纹**与本次 jti 的 `EXISTS`（应为 0）；Ctrl-C 由 trap 兜底。**清理失败的最坏情况**：该 jti 等 TTL 自净，不影响任何后续用例 |
| **浏览器类**（2.4-*） | 浏览器 localStorage 里的登录态；**服务端多一个白名单键**（同一次真登录） | 交互用例必须先登录（否则路由守卫直接弹回登录页） | localStorage 的改动由用例自身产生、重登即回原状；白名单键可点导航栏「退出登录」主动清（等价 TC-01-1.5-5），不清也会 TTL 自净 |
| **TC-02-2.4-4** | **重启 `nexus-backend` 容器** | 全文件里**唯一**能造出"连接在没有任何终止帧的情况下结束"（EOF 第三出口）的手段，没有替代 | 恢复动作**就是用例自身**（`restart` 复用同一容器、同一 IP、同一环境）；自证 = 步骤 ⑤ 的 `compose ps` + `/api/health` 200。**中途被打断的最坏情况**：后端停在那儿 → 下一次任何请求都会响亮失败（不会静默），修法是同一格命令 |
| **TC-02-2.3-5**（**默认不跑**） | 往 `docker-compose/.env.local` 追加 2 行 + 重建后端容器 | 把线程池压到 1，才能用 2 条并发打出 `503`（默认 `max-size=16` 要 17 条并发 + 16 段 CPU 推理） | 用例内自带**还原 + 还原自证**（`sed` 删掉那两行 → `--force-recreate` → `printenv \| wc -c` 期望 0） |
| **TC-02-2.4-9** | 把 `docker-compose/.env.local` 里**所有** `DEEPSEEK_API_KEY=` 行注释掉（**本机 2026-09-21 实测有 2 行同名键**）+ 重建 `nexus-backend` 容器，**并可能连带重启 `nexus-frontend`**（重建换 IP → nginx 启动期解析的 upstream 失效，见该条第 ④ 步） | "容器里没有密钥"是本条要验的前提：环境变量**只在创建容器时注入**（`restart` 读不到），没有更便宜的造法 | 用例内自带**幂等还原**（`sed` 复原那些行 → 再 `--force-recreate` → `printenv \| wc -c` 期望 **> 1**）+ 文件侧自证（`grep -c '^DEEPSEEK_API_KEY=' .env.local` 期望**回到跑前记下的 N**，本机是 **2**，**不是**写死的 1）+ 浏览器链路自证（走 8088 的 `/api/health` 200）。**中途被打断的最坏情况**：`.env.local` 里留下注释行（一眼可辨）+ 容器无密钥 → 云端那半响亮失败、本地那半不受影响；修法 = 同一格的还原命令 |
| **真实调用 Ollama** | 无（读操作） | 对话本身要上游生成 —— 这是本项目演示链路的固有成本 | 无需清理；CPU 占用是本项目的常态 |

**一律不碰**：业务库/业务表/业务数据、`db-patch/`、`build-artifacts` 卷的内容、`scripts/`、
`docker-compose.yml`（2.3-5 与 2.4-9 只碰 `.env.local`，那是 gitignored 的一次性文件）、已发布的契约文档。
**"测试跑完之后环境与跑之前一致"的判据**：上面每一行的自证都通过；2.3-5 若跑过，`.env.local` 里不留下那两行；
2.4-9 若跑过，`.env.local` 里那行 `DEEPSEEK_API_KEY=` 已复原（未被注释）。

### 0.2 需要云端模型时：创建密钥（**只影响 2.1-3 / 2.2-1 ②**）

```bash
cd /mnt/c/wp/nexus-agent-workbench/docker-compose && printf 'DEEPSEEK_API_KEY=%s\n' '<把你的真实密钥替换到这里>' >> .env.local
cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose up -d --force-recreate nexus-backend
cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose exec -T nexus-backend printenv DEEPSEEK_API_KEY | wc -c
```

判据：第 ③ 条打印 **> 1**（值长度 + 换行）；**打印 0 就是没注入成功**，别往下测云端。

> ⚠️ **第 ② 条用 `--force-recreate` 而不是裸 `up -d`**：只改了 `env_file` 指向的文件**内容**时，
> compose 的"配置有没有变"判断**可能**认为没变，于是 `up -d` 是空操作（本项目已实测过 `up -d` 对运行中
> 容器是空操作，见 `docs/design/00` §6.0 的三个反直觉点）。判据是第 ③ 条的字节数 —— 它是 0 就重跑第 ② 条。
> 设计文档 §6.4 给的是裸 `up -d`，此处按"拿不准就 `--force-recreate`"的口诀收紧；已记入 §7 偏差记录。
>
> ⚠️ `.env.local` **不要重复追加**同一行（追加两次无害：同名键后者生效，但会让文件难读）。
> 该文件已被 `.gitignore` 第 3 行覆盖（`git check-ignore -v docker-compose/.env.local` 实测命中），**不会入库**。

### 0.3 脚本清单（步骤栏里那一行命令分别是干什么的）

| 脚本 | 一句话说明 | 用法 |
|---|---|---|
| `qa/scripts/tc02-chat-stream.sh` | **登录 → 发一次 `POST /api/chat/stream` → 把响应头与每一帧带到达时刻原样打印 → 抓 `[chat]` 日志 → 登出 + 指纹**。行为由环境变量调：`TC02_TARGET`（backend/frontend）、`TC02_MODEL_TYPE`、`TC02_PROMPT`、`TC02_BODY`（直接给原始请求体）、`TC02_OMIT_CONTENT_TYPE`、`TC02_MAX_SECONDS` | `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc02-chat-stream.sh` |
| `qa/scripts/tc02-pool-full.sh` | **同时发出 N 条流式对话，逐条打印 HTTP 状态码 / 耗时 / 响应体**（正文截断显示），并打印池的两个环境变量与 `模型线程池已满` 日志 | `cd /mnt/c/wp/nexus-agent-workbench && TC02_CONCURRENCY=2 bash qa/scripts/tc02-pool-full.sh` |
| `qa/scripts/lib/tc02-stamp-lines.py` | 上面那个脚本用的**逐行打时刻过滤器**（它自己也有 `--self-test`：样本里故意含一个 U+FFFD，必须数得出来） | `python3 qa/scripts/lib/tc02-stamp-lines.py --self-test` |

两个脚本的退出码**都不是判据**（0 完成 / 2 用法错 / 3 前提不成立 / 4 登录没拿到 token / 28 客户端放弃 / 130 Ctrl-C 打断）——
**结论一律看它打印的原始响应与日志**。

---

## 1. 后端 provider：两个实现各自可独立完成对话（2.1）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-2.1-1 本地 Qwen 独立完成一次对话 | `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：前置检查 → 打印环境事实与指纹 → 真登录 `admin` → **直连后端 8089** 发一次 `modelType=OLLAMA` 的流式对话（响应头与每一帧**带到达时刻**原样打印）→ 打印后端 `[chat]` 日志 → 登出 + 指纹。**判据看它打印的原始帧，不看退出码** | ① 响应头是 `HTTP 200` + `Content-Type: text/event-stream`（**裸值，不带 `charset`** —— **不要**期望 `;charset=UTF-8`，理由与依据见 §7 第 4 行；脚本第 ⑥ 段能看到两行）；<br>② 汇总的事件计数里有 **`meta:1`**，且该帧的 `data` 是 `{"modelType":"OLLAMA","model":"qwen2.5:7b","servedBy":"user-selected"}`；<br>③ `delta` 帧 ≥ 1（长问答实测通常上百），`data.content` 非空；<br>④ 末帧是 `done`：`finishReason":"stop"`、`deltaCount` = 实际 delta 帧数、`durationMs` > 0；<br>⑤ **没有 `error` 帧**（事件计数里不出现 `error`）；<br>⑥ 汇总里 `严格 UTF-8 解码 = 成功`、`替换字符 U+FFFD 出现次数 = 0` | ✅ **通过**（2026-09-21 用户手工实测；原始捕获 `.result/tmp-result.txt` 第 2 轮，09:17:45 → 09:18:15；**该文件是本地临时文件、未入库**（内含登录 token），关键行已逐字摘录在此。**六条判据逐条有原始帧为证**）<br>① ✅ `HTTP/1.1 200` + `Content-Type: text/event-stream`（裸，无 charset —— 与订正后的判据一致）<br>② ✅ `event:meta` 帧 data 原文：`{"code":0,"msg":"success","data":{"modelType":"OLLAMA","model":"qwen2.5:7b","servedBy":"user-selected"}}`，且汇总里 `meta:1`<br>③ ✅ `delta:425`（≥1），`data.content` 非空（机械拼接出完整正文 **701 字符**）<br>④ ✅ 末帧 `event:done`：`{"finishReason":"stop","deltaCount":425,"durationMs":35106}` —— `deltaCount` 与汇总的 `delta:425` **吻合**、`durationMs > 0`<br>⑤ ✅ 事件计数里**没有** `error`<br>⑥ ✅ `严格 UTF-8 解码 = 成功`、`替换字符 U+FFFD 出现次数 = 0`<br>📌 **取证说明**：09:11 那一次（第 1 轮）因过滤器漏了 `return` 只打出 `None`、判据 ①② 读不到；修好后 09:17 重跑，**本轮才是有效记录**。两条警告也一并留痕：`Transfer-Encoding: chunked` ✓ |
| TC-02-2.1-2 调用链路的 `log.info` 日志 | ① 用上一条那次运行的**第 ⑦ 段**（脚本自己抓的 `[chat]` 行）；<br>② 自己再看一眼：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 100 nexus-backend 2>&1 \| grep -F '[chat]'`<br>③ 隐私佐证：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 200 nexus-backend 2>&1 \| grep -c '杭州'`（把「杭州」换成**你那次实际提问里的一个词**；默认提问里含它） | 三条（设计 §4.5 的前三条）都在，且字段能对上：<br>`[chat] 开始 stream: userId=<非空> tenantId=<非空> modelType=OLLAMA messages=1`<br>`[chat] 路由结果: provider=ollama model=qwen2.5:7b servedBy=user-selected`<br>`[chat] 流结束: provider=ollama deltaCount=<N> durationMs=<M> finishReason=stop`<br>① 第三条的 `deltaCount` 与上一条用例的事件计数里的 `delta` **相等**；<br>② `路由结果` 的 `model` 与 `meta` 帧的 `data.model` **一字不差**（验收 2.2-1 的日志侧证据）；<br>③ 隐私佐证打印 **`0`**（设计 §4.5：只打条数、模型、耗时、分片数，**不打消息正文**） | |
| TC-02-2.1-3 云端 DeepSeek 独立完成一次对话 | **前置**：§0.2 三步都通过（第 ③ 条打印 > 1）<br>`cd /mnt/c/wp/nexus-agent-workbench && TC02_MODEL_TYPE=DEEPSEEK bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：同上，只是把 `modelType` 换成 `DEEPSEEK`（真打 `https://api.deepseek.com/chat/completions`，会产生一次真实计费调用） | ① `meta` 帧的 `data.model` = **`deepseek-chat`**、`modelType` = `DEEPSEEK`、`servedBy` = `user-selected`；<br>② `delta` 有内容、末帧 `done`、**无 `error` 帧**；<br>③ 日志 `路由结果: provider=deepseek model=deepseek-chat …` 与 `流结束: provider=deepseek …` | ✅ **通过**（2026-09-21 用户手工实测；原始捕获 `.result/tmp-result.txt` 第 3 轮，09:21:58 → 09:22:01；同为**本地临时文件、未入库**，关键行已逐字摘录在此）<br>**前置** ✅ 第 ② 段实测"容器内 `DEEPSEEK_API_KEY` 字节数 = **36**"（> 0 ⇒ 密钥确已进容器，§0 那条"没密钥则测不了"的例外**不适用**）；请求体原文含 `"modelType": "DEEPSEEK"`<br>① ✅ `event:meta` 帧 data 原文：`{"code":0,"msg":"success","data":{"modelType":"DEEPSEEK","model":"deepseek-chat","servedBy":"user-selected"}}` —— **直接读到帧**（不是从日志反推）<br>② ✅ `delta:448` 有内容（机械拼接 **653 字符**，正文以「杭州的历史文化，源远流长…」开头）；末帧 `event:done`：`{"finishReason":"stop","deltaCount":448,"durationMs":4034}`；事件计数里**无** `error`<br>③ ✅ 日志两行：`[chat] 路由结果: provider=deepseek model=deepseek-chat servedBy=user-selected` + `[chat] 流结束: provider=deepseek deltaCount=448 durationMs=4034 finishReason=stop`<br>📌 这是**真实云端调用**（`durationMs=4034`，与本地 Ollama 的 35 秒量级截然不同），也是密钥事故修好之后**云端链路的首次协议侧实证** |
| TC-02-2.1-3 备注 | ⚠️ **密钥没进容器时这条"测不了"，不要记成"本条失败"**：那时会得到 **TC-02-2.3-4** 的形态（一帧 `error` + `20100`），而且日志里那两行 warn（`未配置 DEEPSEEK_API_KEY…`）会明确告诉你原因。先回去做 §0.2。<br>⚠️ 上游不可达/密钥无效时**同样**是 `20100` —— 两者靠日志分流：`未配置 DEEPSEEK_API_KEY，本次对话直接按 20100 失败（不发网络请求）` = 环境问题；`DeepSeek 返回错误状态：status=401 …` = 密钥本身的问题。 | 见上 | |

## 2. 路由与入口校验（2.2）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-2.2-1 两个 `modelType` 分别命中对应实现 | ① `cd /mnt/c/wp/nexus-agent-workbench && TC02_MODEL_TYPE=OLLAMA bash qa/scripts/tc02-chat-stream.sh`<br>② `cd /mnt/c/wp/nexus-agent-workbench && TC02_MODEL_TYPE=DEEPSEEK bash qa/scripts/tc02-chat-stream.sh`<br>—— 两步在干什么：各发一次流式对话，看 `meta.model` 与日志 `provider=` 指向谁 | ① OLLAMA：`meta.model=qwen2.5:7b` **且**日志 `provider=ollama`；<br>② DEEPSEEK：**有密钥** → `meta.model=deepseek-chat` **且**日志 `provider=deepseek`；**无密钥**（当前环境）→ 只认日志那两行也能证明**路由命中**：`[chat] 开始 stream: … modelType=DEEPSEEK` + `[chat] 路由结果: provider=deepseek model=deepseek-chat servedBy=user-selected`，随后 `[chat] 流异常结束: provider=deepseek deltaCount=0 … code=20100`（如实失败，见 2.3-4）；<br>③ 两个取值的 `provider`/`model` **必须按请求变**（同一次取值恒定不算通过） | |
| TC-02-2.2-2 未知 `modelType` → 业务异常 | `cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"user","content":"hi"}],"modelType":"XXX"}' bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：把 `modelType` 传成一个契约里没有的值 | ① HTTP **200**（**不是** 4xx/5xx —— 开流前的失败走普通 `Result` 出口，契约 §6.3）；<br>② 响应体是 `{"code":10200,"msg":"不支持的模型类型","data":null}`，`Content-Type` 含 `application/json`；<br>③ **响应体里没有堆栈**（不出现 `at com.` / `Exception` / `Caused by`）；<br>④ 后端日志出现一行 warn：`未知的 modelType：raw=XXX 允许取值=…`；<br>⑤ 汇总里"事件计数 = 一个 `event` 行都没有"（它**不是**事件流 —— 判据是响应**体**，不是帧） | |
| TC-02-2.2-3 `modelType` 缺省 → 后端的默认模型（决策 D6） | `cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"user","content":"你好"}]}' bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：请求体里**不带** `modelType`，看后端会不会自己定 | 正常事件流；`meta` 帧的 `servedBy` = **`default`**（不是 `user-selected`），`model` = `qwen2.5:7b`（= `application.yml` 的 `nexus.ai.default-model` 默认值 `OLLAMA`）；日志 `路由结果: … servedBy=default` | |
| TC-02-2.2-3 备注 | 契约 §6.1 把 `modelType` 写成**可空**（"`null` 或缺省 = 由后端决定"），本条是那一条款的运行期判据，也是设计 §10.1「自动路由」预留缝①的落点。`servedBy` 是本轮唯一能区分"用户选的"与"后端定的"的可观察量。 | 见上 | |
| TC-02-2.2-4 请求体不合法 → `40001`（契约 §6.1 三条补充约束） | ① 空正文：`cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"user","content":"   "}],"modelType":"OLLAMA"}' bash qa/scripts/tc02-chat-stream.sh`<br>② 畸形 JSON：`cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"user"' bash qa/scripts/tc02-chat-stream.sh`<br>③ 非法角色：`cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"system","content":"TC02正文探针"}],"modelType":"OLLAMA"}' bash qa/scripts/tc02-chat-stream.sh` | 三条**都是** HTTP **200** + `{"code":40001,"msg":"请求参数不合法","data":null}` + **响应体无堆栈**；<br>① 日志 `参数校验失败：<字段名>…`（含 `content` 那条约束的字段名）<br>② 日志 `请求体不可读（JSON 畸形或类型不匹配）：…`；两者都是 **warn 级、不打堆栈**；<br>③ 佐证（隐私口径）：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 50 nexus-backend 2>&1 \| grep -c 'TC02正文探针'` → **0**（校验失败的日志**不记字段值** —— 正文属用户数据） | |
| TC-02-2.2-4 备注 | ⚠️ ② 的"畸形 JSON"是**唯一**能测出 `HttpMessageNotReadableException` 出口的形状（设计 §3.4 新增的第二个出口）；③ 的 `role=system` 按契约是 `40001`（**不是** `10200`）——`10200` 只留给 `modelType`。 | 见上 | |

## 3. SSE 流式接口与失败形态（2.3）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-2.3-1 **可见增量分片**（直连后端） | `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：同 2.1-1 的那次运行，本条**看时刻不看内容**（脚本逐行打到达时刻，并在汇总里给出 data 行的首末时刻） | ① 汇总里 `data 行：首个 HH:MM:SS.mmm → 末个 HH:MM:SS.mmm`，**两者的时刻差 ≥ 1 秒**；且逐行时刻里能看到**多个不同的毫秒刻度**（不是同一个）；<br>② 人眼旁证：脚本是**边跑边打印**的 —— 若是"卡两三秒后突然刷一大片"，就是一次性返回，**FAIL**；<br>③ **FAIL 形态的读数**：所有 `data` 行落在**同一毫秒**（或跨度 < 100 ms） | |
| TC-02-2.3-1 备注 | 本条是设计 §9 风险 3「RestClient 是否真**增量**交付」与决策 D1 的**唯一运行期判据**（"响应流已被 drain"那一半已由源码级核实规避，见设计 §12 的 2026-09-20 行）。若一次性返回，第一排查方向是**上游读取**（`exchange` 回调内读流 vs 回调返回后再读），**不是** nginx —— nginx 那半边由下一条分开验。 | 见上 | |
| TC-02-2.3-2 **可见增量分片**（走 nginx 8088） | `cd /mnt/c/wp/nexus-agent-workbench && TC02_TARGET=frontend bash qa/scripts/tc02-chat-stream.sh`<br>—— 这一步在干什么：同上，但目标换成前端端口 8088（经 nginx 反代）。脚本第 ② 段会顺带把容器里 nginx 配置的缓冲那行原样打出来 | ① 与 2.3-1 ① 同判据（data 行首末时刻差 **≥ 1 秒**）；<br>② 第 ② 段打印的行里有 `proxy_buffering off;`（**2026-09-20 实测**：`51: proxy_buffering off;`）；<br>③ **FAIL 形态**：直连（2.3-1）是增量、走 8088 变成一次性 ⇒ 就是 `proxy_buffering off` 没生效（镜像里的 `nginx.conf` 是旧版） | |
| TC-02-2.3-2 备注 | ⚠️ **这是"最容易漏且最难排查"的一条**（设计 §6.5）：nginx 默认把上游响应攒进缓冲区再转发，症状是"等几秒然后整段文字突然出现"，而**后端日志与直连端口完全正常**。<br>修法（**不在本用例范围**，属 devops）：`docker compose build nexus-frontend` → `docker compose up -d nexus-frontend`（`nginx.conf` 是 `COPY` 进镜像的，源码改动重启容器不生效）。<br>⚠️ **不要用 5173（Vite dev）判定 SSE 好坏** —— 那条链路本轮未验（设计 §6.5 末段已约定：SSE 的验收与排查一律走 8088）。 | 见上 | |
| TC-02-2.3-3 **客户端中断 → 资源释放**（curl 侧） | ① `cd /mnt/c/wp/nexus-agent-workbench && TC02_PROMPT='请用 2000 字，分十段，详细介绍中国四大发明。' bash qa/scripts/tc02-chat-stream.sh`<br>② 屏幕上滚出若干 `event: delta` 之后，按 **Ctrl-C**（脚本会打印"（Ctrl-C：读取被主动打断…）"，并照常走清理与指纹 —— 这是设计好的）<br>③ `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 30 nexus-backend 2>&1 \| grep -F '[chat]'`<br>—— ③ 这一步在干什么：看断开那一刻后端记了什么 | ① ③ 的输出里出现**设计 §4.5 的第四行**：<br>`[chat] 客户端断开，已取消上游调用: provider=ollama deltaCount=<N> durationMs=<M>`，<br>其中 `N` = Ctrl-C 前已收到的 delta 帧数（与 ② 屏幕上滚过的帧数同量级）；<br>② **不得**同时出现 `[chat] 流异常结束: … code=20100`（那是"上游不可用"的**假告警**，设计 §4.4 明确要避免）；<br>③ **不得**出现 `[chat] 写帧失败（emitter 已不可用）`（那条是**服务端侧**收尾——硬超时到点，不是客户端断开）；<br>④ **反向判据（本条的真正价值）**：只有断开前那三条、**没有**第四行 ⇒ 验收 2.3-2 不过（客户端已走、服务端既没日志也没关上游）；<br>⑤ 佐证（**取不到不算 FAIL**）：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 5 nexus-ollama` —— 上游是否立即停下，本项目**未实测**（Ollama 的取消行为有没有日志待确认），取不到就写"取不到（不作为判据）" | |
| TC-02-2.3-3 备注 | ⚠️ **主判据是"写帧抛异常"**（`emitter.send()` 失败 → 置取消标志 + 关上游流 + 记这条日志），不是那三个回调：`onCompletion` 在**正常收尾**时同样会触发，在那里记"客户端断开"会凭空造出一条假告警（设计 §4.4）。所以**别**把"日志里有断开"与"回调注册了"混为一谈 —— 本条要的是那条日志。<br>⚠️ 上游确实停止这件事**只能间接佐证**：关流（`InputStream.close()`）即断上游是设计承诺的机制，但 Ollama 侧的可观察痕迹本项目没有留档 —— 如实记"未覆盖"，去向见文末「测不出来的项与去向」。 | 见上 | |
| TC-02-2.3-4 **上游不可用 → `error` 帧 `20100`，且不降级**（决策 D7） | **前置**：容器内**没有** `DEEPSEEK_API_KEY`（`docker compose exec -T nexus-backend printenv DEEPSEEK_API_KEY \| wc -c` → `0`；**2026-09-20 现状即如此**）<br>① （协议侧）`cd /mnt/c/wp/nexus-agent-workbench && TC02_MODEL_TYPE=DEEPSEEK bash qa/scripts/tc02-chat-stream.sh`<br>② （页面侧）浏览器开 `http://localhost:8088` → `admin/admin123` 登录 → 导航栏「对话」→ 模型下拉选「云端 DeepSeek」→ 输入「你好」→ 点「发送」 | ① HTTP **200** + `Content-Type: text/event-stream`；**事件计数 = `error:1`，而 `meta` / `delta` / `done` 全为 0**（成功路径才保证 meta 必有，失败时一个字都没吐就没有 meta —— 契约 §6.2 的边界注）；<br>② 那一帧的 `data` 是**对象**：`{"code":20100,"msg":"模型服务暂时不可用，请稍后重试","data":{"deltaCount":0}}`（**不是** `null`）；<br>③ 后端日志两行：`未配置 DEEPSEEK_API_KEY，本次对话直接按 20100 失败（不发网络请求）` + `[chat] 流异常结束: provider=deepseek deltaCount=0 … code=20100`；<br>④ 页面：气泡下方红字 `生成失败：模型服务暂时不可用，请稍后重试`、输入框上方一条红色 `el-alert` 同文案；"生成中…"在秒级内结束、停止按钮消失；<br>⑤ **防假通过（本条的要害）**：全程**不得**出现任何本地模型的正常回答 —— 事件计数里**不得有 `delta`**，页面上**不得**出现 `qwen2.5:7b` 那个 tag。设计 D7 明说本轮**不做降级**，出现本地回答就是**静默降级** | |
| TC-02-2.3-4 备注 | ⚠️ 缺密钥是**约定的降级**（设计 §6.4）：栈照常起，只影响云端那半。<br>⚠️ **若你已经建了 `.env.local`（§0.2），本条的前置就不成立** —— 那时它会真的调通云端。两条路：① 先跑本条、再建密钥（推荐，顺序最省事）；② 把密钥临时改成一个明显错误的串 → `up -d --force-recreate nexus-backend` → 跑 → 还原 → 再 `--force-recreate`。**② 是「就地注入」**（改的是生产链路消费的配置），要自己确认值得；它的观察点与 ① 相同（`DeepSeek 返回错误状态：status=401 …` 会取代那行"未配置密钥"）。<br>⚠️ 本条与 2.1-3 用的是**同一份环境状态**、判的是**相反的两件事**：2.1-3 要"云端能通"，本条要"云端不通时如实报错、不骗人"。两条都过，才说明"选谁走谁"是真的。 | 见上 | |
| TC-02-2.3-5 **池满 → `503` + `20100`**（**复现成本高，默认不跑**） | **造法 A（推荐：把池压到 1，只要 2 条并发）**<br>① `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && printf 'NEXUS_AI_EXECUTOR_CORE_SIZE=1\nNEXUS_AI_EXECUTOR_MAX_SIZE=1\n' >> .env.local`（文件不存在时这条会创建它；**只有这两行也照常启动**）<br>② `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose up -d --force-recreate nexus-backend`<br>③ `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose exec -T nexus-backend printenv NEXUS_AI_EXECUTOR_MAX_SIZE`（期望打印 `1`）<br>④ `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc02-pool-full.sh`（默认 2 条并发）<br>⑤ **还原**：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && sed -i '/^NEXUS_AI_EXECUTOR_CORE_SIZE=1$/d;/^NEXUS_AI_EXECUTOR_MAX_SIZE=1$/d' .env.local`<br>⑥ `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose up -d --force-recreate nexus-backend`<br>⑦ **还原自证**：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose exec -T nexus-backend printenv NEXUS_AI_EXECUTOR_MAX_SIZE \| wc -c`（期望 `0`）<br>**造法 B（真打默认池，代价高）**：不动配置，`cd /mnt/c/wp/nexus-agent-workbench && TC02_CONCURRENCY=20 bash qa/scripts/tc02-pool-full.sh`（会有 16 段 Ollama 生成同时跑，机器会很忙） | ④ 的输出里：**至少一条** `HTTP/耗时 = 503 …`，其响应体是 `{"code":20100,"msg":"模型服务暂时不可用，请稍后重试","data":null}`（`Content-Type: application/json`）；**其余条**为 `200` + 事件流；<br>⑥ 段日志出现 `模型线程池已满，拒绝本次对话：…pool size…`（warn 级，异常 message 自带线程池现场）；<br>**反面判据**：① 所有条都 200 ⇒ 池没被压小或并发没真正同时（先看 ③/② 段打印的两个字节数）；② 出现"一直转圈而没有报错" ⇒ 变成排队了（队列容量必须为 0）；<br>⑤⑥⑦ 是**无害性**的那半边：必须打印 `0` | |
| TC-02-2.3-5 备注 | ⚠️ **为什么默认不跑**：判据本身风险低（池满是可预测的行为），而代价是重建容器 + 至少一次真实生成。建议把前面几条都跑通再回来。<br>⚠️ **为什么用环境变量而不是改 `application.yml`**：`max-size` 打在 jar 里，改它要重打包（分钟级）且会污染工作区；环境变量经 Spring Boot 的 relaxed binding 覆盖同一个键（`nexus.ai.executor.max-size` ← `NEXUS_AI_EXECUTOR_MAX_SIZE`）。**core 必须一起压到 1** —— `ThreadPoolExecutor` 要求 core ≤ max，只压 max 会让后端**启动失败**。<br>⚠️ **跑过本条会把 `.env.local` 建出来**（里面没有密钥行）——不影响 2.3-4 的前提（它只看 `DEEPSEEK_API_KEY` 在不在），但**如果那个文件里本来就有密钥**，2.3-4 就已经测不了了：顺序上先把 2.3-4 跑掉。<br>（**未实测确认**：`NEXUS_AI_EXECUTOR_MAX_SIZE` 覆盖生效这一点，本文件按 Spring Boot 的既有行为写；第 ③ 步的 `printenv` 与"第 ④ 步到底出不出 503"合起来就是它的自证 —— 出不来就先查第 ③ 步。） | 见上 | |
| TC-02-2.3-6 无 token → 开流前的 `401` 出口 | `curl -s -w '\n%{http_code}\n' -X POST localhost:8089/api/chat/stream -H 'Content-Type: application/json' -d '{"messages":[{"role":"user","content":"hi"}],"modelType":"OLLAMA"}'`<br>—— 这一步在干什么：不带 `Authorization` 打这个接口，看它落哪个出口 | HTTP **401** + `{"code":40100,"msg":"未登录，请先登录","data":null}` + `Content-Type: application/json;charset=UTF-8`（**不是** 302/403/事件流）。<br>**2026-09-20 实测**：直连 `8089` 与走 `8088` 两条链路的响应体、状态码逐字相同（都带 `application/json`）—— 这正是前端 `chat.ts` 敢按 `Content-Type.includes('application/json')` 分流的现实依据（契约 §6.3：`20100` 有两种载体） | ✅ **通过**（2026-09-20 qa 侧只读探针，直连 8089 与走 8088 各一次；**非用户验收**。两条链路的响应头与响应体原文见会话记录，未截图。⚠️ 本条是**零写操作**的：探针不带 token，不会写 Redis） |

## 4. 前端对话页（2.4）

> 本节是**操作类**用例（浏览器交互），步骤写成逐步编号动作；每条都先给"这一步在干什么"，再给"立刻能看到什么"。

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-2.4-1 下拉切换模型 + 实时打字机输出 | ① 浏览器打开 `http://localhost:8088`，用 `admin/admin123` 登录（落到 `/dashboard`）<br>② 点导航栏「对话」（或地址栏输入 `http://localhost:8088/chat`）<br>③ 确认下拉是「本地 Qwen（Ollama）」，输入「用三句话介绍杭州」，回车（或点「发送」）<br>④ **盯住回答气泡**：文字应逐字/逐段冒出来，不是整段突然出现；生成期间「发送」置灰显示「生成中…」、旁边出现红色「停止生成」<br>⑤ 结束后看气泡右上的灰色 tag<br>⑥ 下拉切到「云端 DeepSeek」，输入「用三句话介绍北京」，发送，重复 ④⑤ | ① 两次都是**逐字出字**（判据是"看得见它在往外吐"，不是"最终文本对不对"）；<br>② tag 分别是真名：本地 **`qwen2.5:7b`**、云端 **`deepseek-chat`**（tag 来自 `meta` 帧的 `model`，不是前端常量 —— 这是验收 2.2-1 的界面侧证据）；<br>③ 流式期间下拉是**禁用**的、发送按钮**置灰**；结束后立刻恢复；<br>④ 无密钥时第 ⑥ 轮按 2.3-4 的形态失败（tag 不出现），**不算本条失败** | 已执行（未报告失败）—— 2026-09-20 用户手工实测 |
| TC-02-2.4-2 多轮上下文保持 | ① 在对话页输入「请记住这个数字：7391。只要回复"好的"两个字。」发送，等生成结束<br>② 再输入「我刚才让你记住的数字是多少？」发送 | ② 的回答里**出现 `7391`**；页面上两条 user 气泡都在。<br>**判据只判"引用了第一轮的内容"**，不判措辞与格式（7B 小模型）。若答非所问，先把提问改得更直白（如"7391 是多少？"）再试一次 —— 本轮仍须引用第一轮上送的内容才算过 | 已执行（未报告失败）—— 2026-09-20 用户手工实测 |
| TC-02-2.4-2 备注 | 后端**无状态**（决策 D9）：上下文完全靠前端把 `messages` **全量上送**。所以本条同时也是"全量上送确实发生了"的运行期判据。<br>刷新页面 → 历史清空（页面顶部有 `本阶段对话不落库…` 的提示条），**属预期**（D9 的代价，见 2.4-8 ②）。 | 见上 | |
| TC-02-2.4-3 **中文不乱码**（含"必须用长回答"） | ① 在对话页**连续问 3~5 个会引出长回答的中文问题**，每轮等生成结束再问下一轮，例如：<br>　「请用大约 500 字介绍杭州的历史文化，分四段」<br>　「请用大约 500 字介绍西湖十景的由来」<br>　「请用大约 500 字介绍一下宋韵文化」<br>② 每轮之后按 **F12 → Console**，粘这条命令回车（`0xFFFD` 就是那个"替换字符"的编号 —— 用编号写是为了**粘过去不会变形**；这条命令把整块对话区的文字取出来查有没有那个字符）：<br>　`document.querySelector('.chat-list').innerText.includes(String.fromCharCode(0xFFFD))`<br>③ （对照）在 WSL 里用**同一个提问**跑一次：<br>　`cd /mnt/c/wp/nexus-agent-workbench && TC02_PROMPT='请用大约 500 字介绍杭州的历史文化，分四段' bash qa/scripts/tc02-chat-stream.sh` | ① ② 每一轮 Console 都打印 **`false`**（出现 `true` ⇒ 页面上有 U+FFFD，FAIL）；<br>③ 汇总里 `严格 UTF-8 解码 = 成功` **且** `替换字符 U+FFFD 出现次数 = 0`；<br>④ 再肉眼通读一篇长回答，屏幕里**没有替换字符**（那个字形是个黑色菱形问号）；<br>⑤ 分流判据：③干净而页面有替换字符 ⇒ **前端解码**问题（`TextDecoder` 少了 `{stream:true}`）；③自己就不干净 ⇒ 问题在服务端/上游 | 已执行（未报告失败）—— 2026-09-20 用户手工实测 |
| TC-02-2.4-3 备注 | ⚠️ **短回答不算跑过本条**：一个汉字被 TCP 分片劈成两半是**概率事件**，短回答碰巧不出现。判据只在**长回答 + 多轮**（3~5 轮）时成立 —— 这也是任务书点名"必须用长回答"的原因。<br>⚠️ 这是设计 §5.1-4 那条**静默坑**的唯一防线：`TextDecoder` 少 `{stream:true}` 时被劈开的汉字会解成 U+FFFD **且不抛异常**，而本阶段**不引 Vitest**（设计 §10.2）—— 机器测不出来，只能靠这一条。<br>⚠️ **② 不必每轮查一次（2026-09-20 订正）**：`.chat-list` 是**累积容器**（`ChatView.vue:264` 的 `v-for` over `bubbles`），`innerText` 会把**全部轮次的气泡**一起取出 ⇒ **问完全部轮次后查一次即覆盖全部内容**，与逐轮各查一次等价。原步骤写"每轮之后"是多余的 DevTools 操作（3~5 轮 = 3~5 次），判据不变。<br>⚠️ **`false` 的前提是"页面上确实有长回答的文字"**：空页面同样返回 `false`（**假通过**）。粘之前先跑 `document.querySelector('.chat-list').innerText.length` 确认是**几百**量级；两位数或 0 说明还没渲染完。 | 见上 | |
| TC-02-2.4-4 流的三出口之 **EOF**（生成中途后端被杀） | **作用域声明**：本条会**重启 `nexus-backend` 容器**（服务短暂不可用十几秒）。为什么非做不可：EOF（连接在**没有任何终止帧**的情况下结束）是三个出口里唯一"平时不会自然发生"的那条；谁在消费：除本用例的浏览器外**没有别的客户端在跑**（跑前确认没有其他标签页在流式对话）；恢复动作就是用例自身，自证在第 ⑤ 步。<br>① 浏览器开 `http://localhost:8088` → 登录 → `/chat` → 下拉选「本地 Qwen」<br>② 输入「请用 2000 字，分十段，详细介绍中国四大发明」→ 发送<br>③ **看到气泡里已经有文字之后**，在 WSL 里执行：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose restart nexus-backend`<br>④ 回到浏览器**盯住页面**（不要刷新）：看"生成中"多久结束、按钮怎么变、气泡上出现什么<br>⑤ 恢复自证：`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose ps nexus-backend`（先 `Up … (health: starting)`，几十秒后 `healthy`）与 `curl -s localhost:8089/api/health`（200） | ① 页面**不卡死**：在**数秒内**（肉眼可接受的等待；**具体秒数未实测**，取决于 nginx 何时察觉上游 EOF —— 别把"慢了几秒"读成 FAIL）退出"生成中"、停止按钮消失、"发送"恢复可用；<br>② 该轮气泡被标记失败（红字 `生成失败：…`），文案是下面**两条之一**（**两条都算通过**，但**必须记下是哪一条**）：<br>　· `生成失败：连接已中断（未收到结束帧），本次回答可能不完整` —— 走的是**第三出口（EOF）**，即设计 §5.4 要守的那条；<br>　· `生成失败：无法连接后端服务（服务未启动或网络不通）` —— 浏览器把这条被掐断的 body 判成了网络错误（读取时抛错，走 `catch` 分支，**同样不卡死**）；<br>③ 已吐出的文字**保留**在气泡里（不被清空）；<br>④ **FAIL 形态**：页面一直停在"生成中"、"停止生成"始终在（只认 `done`/`error` 的实现就会这样） | ✅ **通过**（2026-09-21 用户手工实测；造法 = 步骤 ③ 原文的 `docker compose restart nexus-backend`）<br>⚠️ 判据 ② 点名「**必须记下**」的红字文案（**EOF 那条** vs **网络错误那条**）**未回传** —— 待补，**不按预期反推**<br>恢复自证（步骤 ⑤）：2026-09-21 07:36 AI 侧复核 → `nexus-backend Up (healthy)`、直连 `localhost:8089/api/health` **200** + `postgres/redis/ollama` 三项 UP（`version=0.2.2`）；顺带补一条**走 nginx 的** `http://localhost:8088/api/health` 亦 **200** ⇒ 上游 IP 未失效（`restart` 不换 IP，正是本条备注要的那个形态），浏览器那条链路当时是通的 |
| TC-02-2.4-4 备注 | ⚠️ **为什么必须重启容器**：没有别的手段能造出"连接在**没有终止帧**的情况下结束"（后端不落库、无法用数据造；前端固定走 `/api/chat/stream`，没法指向桩服务）。它是本文件唯一一处**碰共享依赖**的动作，所以作用域、恢复与自证都写在步骤里。<br>⚠️ 用 `restart`（**不要** `stop` + `up`，也不要 `--force-recreate`）：`restart` 复用同一容器 → **IP 不变** → nginx 启动期解析好的 upstream 仍然有效；换 IP 会看到 502，那是另一个形态。<br>⚠️ **更便宜但不能替代的等价造法**（可选，只覆盖 `catch` 分支）：DevTools → Network → 把 "No throttling" 改成 **Offline** —— 文案应是 `无法连接后端服务（服务未启动或网络不通）`；它证明"页面不卡死"，**证不了 EOF 那条分支**。<br>⚠️ **未实测确认**：这一步到底落到 ② 的哪一条，取决于 nginx 如何结束这条被上游掐断的响应（干净收尾 vs 直接断连），本项目没有留档 —— **首次执行时把实际是哪一条记进「实测」栏**，别按预期反推。<br>✅ **2026-09-21 首次执行后的实测附注**（造法核实）：确为用户手工执行 `docker compose restart nexus-backend`。容器时间戳佐证 —— `nexus-backend` 的 `Created=2026-09-20T23:32:27Z` 与 `StartedAt=2026-09-20T23:35:05Z`（本地 07:32:27 / 07:35:05）**不是同一时刻**：容器对象没被重建、只是被停掉又起来（若用 `--force-recreate`，`Created` 会≈`StartedAt`；那会换 IP → 502，是另一种形态）。<br>⚠️ **取证教训（别再用错判据）**：该容器 `RestartCount=0`，但 **`RestartCount` 只统计「重启策略」触发的自动重启，用户手工 `docker restart` / `compose restart` 不计入** —— 拿它判「是否 restart 过」会得出反的结论（本次 AI 侧据此误报过一次矛盾，已更正）。**判据永远是 `Created` vs `StartedAt`**。<br>⚠️ **仍然悬着的一项**：因为 ② 的文案未回传，「**桌面端页面侧实际走的是 EOF 出口还是 catch 出口**」至今**没有实测值** —— 这正是本备注最后一条「未实测确认」说的那件事，未因本次通过而消解；下次重跑时顺手记一次即可（任务书里它是可选项，不影响本行判据）。 | 见上 | |
| TC-02-2.4-5 **停止生成**（AbortError 分流）+ 连续同角色 | ① 在 `/chat` 发一个长回答（「请用 2000 字，分十段，详细介绍中国四大发明」）<br>② 等气泡里出现文字后，点红色「停止生成」<br>③ 立刻看四处：正文是否停住、气泡下方多出什么、两个按钮怎么变、屏幕上有没有红条<br>④ 紧接着再问一轮（例：「刚才说到哪了？」）→ 看是否正常出字<br>⑤ （契约侧的等价验证）`cd /mnt/c/wp/nexus-agent-workbench && TC02_BODY='{"messages":[{"role":"user","content":"第一个问题"},{"role":"user","content":"第二个问题"}],"modelType":"OLLAMA"}' bash qa/scripts/tc02-chat-stream.sh` | ③ 四点全中：正文立刻停止增长；气泡下方出现**灰字** `已停止生成（回答不完整）`；「停止生成」消失、「发送」**立刻可用**；**屏幕上不得出现任何"无法连接后端服务…"的红条/红字**（设计 §5.4 点名的高频翻车点）；<br>④ 正常出字、不报错（这一轮正是"连续两条 user"的形状：上一轮被停止的 assistant 保留、这轮又 push 一条 user）；<br>⑤ 正常事件流（有 `delta`/`done`），**不返回 `40001`** —— 连续同角色是契约**允许**的（§6.1 补充约束 1） | ✅ **通过**（2026-09-21 用户手工实测）<br>⚠️ **未回传、不代填**：① 观察到的是哪一种合法形态 —— **流中途停止**（灰字「已停止生成（回答不完整）」）还是**一个字都没吐就停**（回滚整轮 + 文字回到输入框）；② 步骤 ⑤ 那条**契约侧** curl（`TC02_BODY=…连续两条 user…`）**跑没跑** —— 若没跑，则"连续同角色**不返回 `40001`**"这一条至今**只有界面侧的间接证据**，协议侧仍缺 |
| TC-02-2.4-5 备注 | ⚠️ 停止**不是失败**：实现把 `AbortError` 与真实失败分成两个回调，若没分流，用户每次点停止都会看到一条"请求失败/无法连接后端"（设计 §5.4 点名）。<br>⚠️ **一个字都没吐出来时点停止**是另一条合法形态：实现会**回滚整轮**（连那条 user 一起）并把文字放回输入框（设计 §5.5）—— 那时**不会**出现"已停止生成"灰字，判据变成"输入框里恢复了刚打的字"。两种形态都合法，别把其中一种记成失败。<br>⚠️ 「已停止生成（回答不完整）」这行字是**展示字段**，不写进 `message.content` —— 否则下一轮全量上送时会被当作模型输出喂回模型（设计 §5.5 要避免的事）。观众看不出区别，但它会在 2.4-2 那类多轮用例里以"模型开始自问自答"的形式露头。 | 见上 | |
| TC-02-2.4-6 本页**首请求必须成功**（`Content-Type` 陷阱） | ① 在 `/chat`（确保是登录态）**第一次**就发一句「你好」<br>② 若出现失败：把红字原文原样记下，并跑下面的诊断：<br>　`cd /mnt/c/wp/nexus-agent-workbench && TC02_OMIT_CONTENT_TYPE=1 bash qa/scripts/tc02-chat-stream.sh`<br>—— ② 这一步在干什么：**刻意不发 `Content-Type`**，用 curl 复现"前端漏了那个头"时后端到底怎么回 | ① 首次发送就成功、页面无任何红色提示（这是本页的门槛判据：`fetch` 不走 axios 拦截器，两个头都得自己补）；<br>② **诊断段的实测形态（2026-09-20 只读探针）**：本后端对 media-type 不匹配（`fetch` 不带这个头时会自动打 `text/plain;charset=UTF-8`）返回的是 **HTTP 500 + `{"code":50000,"msg":"系统繁忙，请稍后重试","data":null}`**（探针打在 `/api/auth/login` 上 —— 同为 `consumes=application/json` 的白名单接口、走同一个兜底处理器），**而不是**契约 §6.1 写的"415 且响应体不是 Result"；那时页面文案会是 `生成失败：系统繁忙，请稍后重试`，把排查方向带偏成"服务端故障"；<br>⇒ 看到的**不是** 415 时，**不要**判成"用例写错了"：判据只有 ①，② 是定位手段；把实际状态码与响应体原文记录下来（见 §7） | ✅ **通过**（2026-09-21 用户手工实测；判据是 ①，即**首次发送就成功、页面无红字**）<br>⚠️ **未回传、不代填**：步骤 ② 那条诊断命令（`TC02_OMIT_CONTENT_TYPE=1`）**本次跑没跑** —— 它不判通过、只记录形态。若跑了，把状态码与响应体原文补进 §7 第 1 行；若没跑，§7 第 1 行仍是 2026-09-20 只读探针的值（`HTTP 500 + 50000`）。<br>📌 **连带提醒**：该值**会随 `0f4c1e0` 的部署而变成 `415`**（那个提交正是为此加的专门出口，**目前仍未部署**）—— 部署后 §7 第 1 行与设计 §5.1-2 / 契约 §6.1 的"415"就重新对齐了，届时要一并更新，别让旧实测值挂在文档里 |
| TC-02-2.4-7 对话页的 `401` 统一处置（fetch 链路） | **为什么不能"把 localStorage 的 token 改坏再发送"**：`/chat` 是受保护路由，刷新后**路由守卫**会先调 `GET /api/auth/me`（axios 链路），token 一坏就在**进页面之前**被弹回登录页（那是 TC-01-1.5-3 已覆盖的形态），**根本走不到本页的发送**。所以改用"页面正常、把**服务端**这条登录态作废"的造法：<br>① 登录后进 `/chat`，正常发一条消息（确认页面是好的）<br>② **新开一个标签页**（Ctrl+T）→ 地址栏 `http://localhost:8088` → 页面进去后点导航栏右上「退出登录」<br>　（同域共享 localStorage，但**第一个标签页内存里的 token 仍是旧的**，而服务端白名单里那条已被删掉）<br>③ 回到**第一个**标签页（**不要刷新**），输入「你好」→ 点「发送」 | ① 自动跳转登录页，地址栏是 `http://localhost:8088/login?redirect=/chat`（**只跳一次**，不出现跳两次/来回闪）；<br>② F12 → Application → Local Storage 里 `nexus.auth.token` 已被清空；<br>③ 在登录页用 `admin/admin123` 重新登录 → **回到 `/chat`**（不是 `/dashboard`）；<br>④ 该轮不会留下"生成中"（离开页面即结束）；<br>**FAIL 形态**：拿到 401 却**没跳**登录页（气泡会显示 `生成失败：登录状态无效，请重新登录`）—— 说明 fetch 链路的 401 只当普通失败处理了 | ✅ **通过**（2026-09-21 用户手工实测）<br>⚠️ **未回传、不代填**：四条判据（单次跳转 + `redirect=/chat`、localStorage 里 `nexus.auth.token` 已清、重登回到 `/chat`、不留"生成中"）的逐条观察值。**本条本就覆盖不到的**：防抖标志的并发形态（本阶段造不出两条并发 401，见备注） |
| TC-02-2.4-7 备注 | 本条证的是「**fetch 链路**的 401 也走同一处处置」——实现把 `handleUnauthorized` 连同**模块级防抖标志**一起抽到 `utils/authFailure.ts`，axios 与 fetch 两个入口 import 同一个函数对象。<br>⚠️ **本条覆盖不到的**：防抖标志的**并发**形态（同一批并发请求只跳一次）。本页在任何时刻只有 SSE 一条请求，造不出两条并发 401（要覆盖它得有一个同时发 axios + SSE 的页面，本阶段没有）。观察点因此降级为"不出现重复跳转 + `redirect` 参数正确"。 | 见上 | |
| TC-02-2.4-8 新增路由 `/chat` 的直达与刷新 | ① （已登录）地址栏输入 `http://localhost:8088/chat` 回车<br>② 在 `/chat` 页面按 **F5**<br>③ 点导航栏「退出登录」后，再地址栏输入 `http://localhost:8088/chat` | ① 直接进对话页（不 404、不被 SPA 回退吞掉）；<br>② 刷新后仍在 `/chat`；页面顶部有 `本阶段对话不落库` 的提示条；历史清空（D9 的预期行为，不是缺陷）；<br>③ 跳 `/login?redirect=/chat`（该路由**没有**加 `public`，由既有守卫处理） | ✅ **通过**（2026-09-21 用户手工实测）<br>⚠️ **未回传、不代填**：三条判据的逐条观察值（① 不 404、不被 SPA 回退吞掉；② 刷新后仍在 `/chat` + 顶部提示条在 + **历史清空属 D9 预期、不是缺陷**；③ 跳 `/login?redirect=/chat`） |
| TC-02-2.4-9 **不配密钥时页面如实报错**（不静默降级 —— 决策 D7） | **作用域声明**：本条会改 `docker-compose/.env.local` 的**一行**（把 `DEEPSEEK_API_KEY` 注释掉）并**重建 `nexus-backend` 容器**（服务短暂不可用）；重建**可能换容器 IP**，因而可能连带重启 `nexus-frontend`（第 ④ 步先验链路）。为什么非做不可：环境变量**只在创建容器时注入**，`restart` 读不到新值 —— "容器里没有密钥"没有更便宜的造法；谁在消费：除本用例的浏览器外**没有别的客户端在跑**（跑前确认没有其他标签页在流式对话）。<br>① 停用密钥（把**所有** `DEEPSEEK_API_KEY=` 行注释掉 —— ⚠️ **本机 2026-09-21 实测该文件有 2 行同名键**，故"改那一行"的说法对本机不成立；`sed` 不带 `g` 是"每行第一处"，多行照样逐行命中）：<br>　(a) **先记下原始行数**（第 ⑨(b) 步要拿它当还原判据，**不要写死 1**）：<br>　　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && grep -c '^DEEPSEEK_API_KEY=' .env.local`<br>　(b) 停用：<br>　　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && sed -i 's/^DEEPSEEK_API_KEY=/#TC02-2.4-9-DISABLED DEEPSEEK_API_KEY=/' .env.local`<br>　—— 这一步在干什么：compose 的 `env_file` 不认注释行 ⇒ 容器里就没有这个变量了。**文件缺失时 `sed` 报错**；但**一行都没有时 `sed -i` 不报错、也不改动** —— 那种情况下第 ③(b) 步的 `0` 是"因为本来就没有"而成立的假前提，先按 §0.2 把密钥建好再回来。<br>② 重建后端容器（让新环境变量生效）：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose up -d --force-recreate nexus-backend`<br>③ **停用自证**（两条都要，任一条不是 0 就别往下测）：<br>　(a) `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose exec -T nexus-backend printenv DEEPSEEK_API_KEY \| wc -c`（期望 **`0`**）<br>　(b) `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && grep -c '^DEEPSEEK_API_KEY=' .env.local`（期望 **`0`**）<br>④ **先验浏览器链路**（`--force-recreate` 可能换 IP，而 nginx 的 upstream 是启动期解析的）：<br>　`curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8088/api/health`<br>　—— 期望 **`200`**；不是 200 就 `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose restart nexus-frontend`，再跑一次这条 `curl`；**仍不是 200 就停下别测**（那是链路故障，不是本用例要验的形态）。<br>⑤ 页面步骤：<br>　(a) 浏览器开 `http://localhost:8088`，用 `admin/admin123` 登录（落到 `/dashboard`）<br>　(b) 点导航栏「对话」进 `/chat`<br>　(c) 模型下拉选「**云端 DeepSeek**」<br>　(d) 输入「你好」→ 点「发送」<br>　(e) 盯住四处：① 气泡区 ② 「发送」/「停止生成」两个按钮 ③ **输入框上方** ④ 输入框本身（另看一眼**屏幕顶部居中**的 toast）<br>⑥ 佐证（后端日志；与 2.3-4 ③ 是同一条判据）：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose logs --tail 30 nexus-backend 2>&1 \| grep -F '[chat]'`<br>⑦ **幂等还原**（可重复跑；漏跑这一步的后果见备注）：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && sed -i 's/^#TC02-2.4-9-DISABLED DEEPSEEK_API_KEY=/DEEPSEEK_API_KEY=/' .env.local`<br>⑧ 重建后端，让它吃到还原后的密钥：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose up -d --force-recreate nexus-backend`<br>⑨ **还原自证**（两条都要）：<br>　(a) `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && docker compose exec -T nexus-backend printenv DEEPSEEK_API_KEY \| wc -c`（期望 **> 1**）<br>　(b) `cd /mnt/c/wp/nexus-agent-workbench/docker-compose && grep -c '^DEEPSEEK_API_KEY=' .env.local`（期望 **回到第 ①(a) 步记下的那个 N** —— 本机当前是 **2**；若打出 `1`，说明只还原了一行、另有一行还注释着）<br>⑩ 再验浏览器链路（同 ④，期望 **200**；不是就 `restart nexus-frontend` 再验）—— 别让用例跑完把浏览器链路留在 502 | ① **防假通过（决策 D7 的要害）**：屏幕上**不得出现任何 Qwen 的回答**、也**不得出现 `qwen2.5:7b` 的 tag** —— 出现即 **FAIL**（那是"选了云端、跑成了本地"的静默降级）；<br>② 失败如实落到界面，形态是**整轮回滚**（设计 §5.5 里"该轮 assistant 一个字都没吐"那半）：提问的 `我` 气泡与空白的 `模型` 气泡**都消失**；若本条是页面的**第一轮**，气泡区回到 `输入问题开始对话（当前历史为空）` 的空态；**气泡下方不会有红字**（气泡已被回滚，没有东西可挂）；<br>③ 红字在**两个别处**，文案逐字 `模型服务暂时不可用，请稍后重试`（= 后端 `20100` 的 msg）：**输入框上方**一条红色 `el-alert`（**不带** `生成失败：` 前缀）+ 屏幕**顶部居中**一条同文案的红色 toast（`ElMessage.error`，**几秒后自动消失** —— 判据以 el-alert 为准）；<br>④ 输入框里**恢复了刚打的「你好」**（回滚的副产品；设计 §5.5 要求"用户打的字不蒸发"）；<br>⑤ 按钮：`发送` 立刻恢复可用（文字回到 `发送`、不是灰的）、`停止生成` **看不到**（本场景后端不发网络请求，毫秒级就结束）；<br>⑥ 佐证（后端日志两行）：`未配置 DEEPSEEK_API_KEY，本次对话直接按 20100 失败（不发网络请求）` + `[chat] 流异常结束: provider=deepseek deltaCount=0 … code=20100 msg=模型服务暂时不可用，请稍后重试`；<br>⑦ **FAIL 形态**：页面一直停在「生成中…」/「停止生成」常驻；或红字文案是**别的**错误码（`请求参数不合法` = `40001`、`系统繁忙，请稍后重试` = `50000`）；或出现 ① 说的本地回答 / 本地 tag | ✅ **通过**（2026-09-21 用户手工实测；判据 ① 的**防假通过**由用户确认 —— 屏幕上没有 Qwen 回答、也没有 `qwen2.5:7b` 的 tag）<br>⚠️ **未回传、不代填**：判据 ②③④⑤ 的逐条观察值（回滚形态 / 两处红字文案 / 输入框是否恢复「你好」/ 按钮是否复位）<br>⚠️ **判据 ⑥ 未实记**：旧容器日志**随重建丢失**，`未配置 DEEPSEEK_API_KEY` 与 `DeepSeek 返回错误状态` 两条在当前容器日志里**都查不到** ⇒ 到底落到哪条形态**无从实记**。文件痕迹（2 条 `#TC02-2.4-9-DISABLED` 注释行）**提示**当时两行都被 ①(b) 注释过 ⇒ 应为「未配置」形态 —— 但这是**推断，不是实测**（本文件纪律：别按预期反推）<br>⛔ **无害性自证未达成（第 ⑨ 步）**：跑完 `.env.local` 里**未注释的** `DEEPSEEK_API_KEY=` 行数是 **1**（⑨(b) 期望回到跑前记下的 **N=2**），且留着 **2 条** `#TC02-2.4-9-DISABLED` 注释行 ⇒ **环境与跑之前不一致**（收拾命令见备注）<br>✅ **但结果比跑之前更好，且当天已由用户手工收拾干净（2026-09-21 复核）**：`.env.local` 现在只剩 **1 行**生效的 `DEEPSEEK_API_KEY=`（35 字符、`sk-` 前缀，md5 前 8 = `79f033c8`），**无残留注释行、无同名重复行**；容器吃到的正是这一行（与文件同 md5 ⇒ **无需重建**）；链路 8088 / 8089 均 **200**。⇒ 净效果：那条 14 字符的**占位值被彻底移除**（不是绕开），云端从"坏"回到"可用"（**有效性尚未验证**，`2.1-3` 一跑见分晓）<br>✅ 链路自证（判据 ④/⑩ 的形态）：走 nginx `http://localhost:8088/api/health` 与直连 `localhost:8089/api/health` **均 200** |
| TC-02-2.4-9 备注 | ⚠️ **只有一条出口，不要按"两条都算通过"写**：`DeepSeekService.stream()` 的密钥检查在**工作线程**上（由 `ChatServiceImpl.runStream` 调用），那时 emitter **已经开流** ⇒ 只能走 `event: error` 帧（契约 §6.3 的"全异步"口径）。开流前的 `Result` 20100（HTTP 503）那条路**只属于池满**（2.3-5）—— **若真看到 503 + `{"code":20100,…}`，那是池满，另记，别当本条通过**。<br>⚠️ **没有 `meta` 帧是预期**：`error` 路径刻意不补发 meta（契约 §6.2 的边界注），所以"没有 tag"不是缺陷；与判据 ① 不矛盾 —— 那里说的是"不该出现**本地模型名**的 tag"。<br>⚠️ **第 ④ / ⑩ 步为什么非加不可**：`--force-recreate` 会换容器 IP，而 nginx 的 `proxy_pass http://nexus-backend:8089;` 是**启动期解析**（配置里没有 `resolver`，见 `docker-compose/frontend/nginx.conf`）⇒ 重建后 nginx 还在打旧 IP，浏览器走 8088 会 **502**，页面显示的是传输层文案（如 `无法连接后端服务…`），**不是本条要验的 20100**。（2.4-4 能躲开这个坑是因为它用 `restart`：复用容器、IP 不变；本条改的是 `env_file`，**只能重建**。更省事的等价做法：第 ② 步把前后端一起重建 —— `docker compose up -d --force-recreate nexus-backend nexus-frontend`，前端重建时自然会重新解析，代价是多等一次 `service_healthy`。）<br>⚠️ **还原为什么是两格（⑦ 改文件 + ⑧ 重建）**：`sed` 只改文件，而环境变量**只在创建容器时注入** ⇒ 不重建，容器里仍是"没有密钥"（同 2.3-5 的道理，`restart` 不够）。还原自证的两条**缺一不可**：`printenv` 证明容器吃到了、`grep -c` 证明文件里没留下注释行。<br>⚠️ **与 2.3-4 的边界**：2.3-4 判的是**协议侧**（`error` 帧 + `20100` + 不降级），它的页面半是一句粗判；本条是**界面侧**的细化（回滚形态 / 两处红字 / 输入框恢复 / 按钮复位）。两条用同一份环境状态、可以一次造法把两条都跑掉，但**别把其中一条的结论当成另一条已跑**。<br>⚠️ **本条与 2.3-4 ④ 的措辞不一致（按代码写，未改 2.3-4）**：2.3-4 ④ 写"气泡下方红字 `生成失败：模型服务暂时不可用，请稍后重试`"，但按 `ChatView.vue` 的 `settleRound`（= 设计 §5.5 的"assistant 文本为空则回滚"），**缺密钥时一个字都没吐 ⇒ 走的是回滚分支：没有气泡，也就没有那行红字**（`生成失败：` 前缀只在"已经吐出过文本"的失败轮次里出现）。本条按**代码里的真实形态**写判据 ②③；2.3-4 ④ 原样留着，待它实测时订正。<br>⚠️ **同名键的覆盖顺序是个陷阱（2026-09-21 实测）**：`.env.local` 里重复的 `DEEPSEEK_API_KEY`，**后面的行覆盖前面的行**（判据是容器里的值与哪一行的 md5 相同）。后果很隐蔽 —— 往里追加一行占位符，真密钥就被静默顶掉，而启动日志照样打 `apiKey已配置=true`，直到第一次云端请求才以 `401 ⇒ 20100` 现形（**不是**"未配置"那条）。所以第 ①(a) 步要**数行**、第 ⑨(b) 步要**按 N 还原**，别写死数字。<br>🧹 **现场收拾：已于 2026-09-21 由用户手工完成**（文件现有 1 行生效键、无残留），下面两格**留档供下次参考**：(a) **只去注释**（把本用例注释掉的行还原；最后一行若仍是真密钥 ⇒ 云端照常可用）：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && sed -i 's/^#TC02-2.4-9-DISABLED DEEPSEEK_API_KEY=/DEEPSEEK_API_KEY=/' .env.local`<br>　(b) **清掉"陷阱行"**（不以 `sk-` 开头的占位值 —— 谁把它挪到最后一行，云端就又坏了；本次就是这样被遮蔽的）：<br>　`cd /mnt/c/wp/nexus-agent-workbench/docker-compose && sed -i '/^DEEPSEEK_API_KEY=/{/sk-/!d}' .env.local`<br>　两格都是**改文件**；要让结果进容器仍得 `up -d --force-recreate nexus-backend`（环境变量只在创建时注入）。**改密钥文件属用户资产** —— 执行者不自作主张，交给用户决定。<br>📌 **下一次跑本条前必须先数行**（①(a)）：文件的形态已经变了（现在 3 行：2 条注释 + 1 条生效），写死数字的判据会当场失效。 | 见上 | |

## 5. 回归（不得破坏既有交付物）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-R-1 健康检查不受影响 | `curl -s localhost:8089/api/health` | HTTP 200 + `code=0` + `checks` 三项全 `UP`（**2026-09-20 实测**：postgres/redis/ollama 全 UP、`version=0.2.2`） | ✅ **通过**（2026-09-20 qa 侧只读探针；**非用户验收**，原始响应见会话记录） |
| TC-02-R-2 巡检无新增失败 | `cd /mnt/c/wp/nexus-agent-workbench && ./scripts/check-health.sh; echo "exit=$?"` | FAIL 数为 0（阶段0/1 基线 `PASS 20 / WARN 0 / FAIL 0`，见 `TC-00-0.3-3`）；`exit=0` | |
| TC-02-R-3 登录与 401 三态不受影响 | ① 无 token：`curl -s -w '\n%{http_code}' localhost:8089/api/auth/me`<br>② 伪造 token：`curl -s -w '\n%{http_code}' -H 'Authorization: Bearer forged.token.value' localhost:8089/api/auth/me`<br>③ 过期 token（**两条要在同一个 shell 里跑**，② 用 ① 存下的 `$TOKEN`）：<br>　(a) `TOKEN=$(bash /mnt/c/wp/nexus-agent-workbench/qa/scripts/tc01-expired-token.sh) && echo "token 长度=${#TOKEN}"`<br>　(b) `curl -s -w '\n%{http_code}' -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/me`<br>④ 正常登录：`curl -s -X POST localhost:8089/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}'` | ① 401 + `40100`；② 401 + `40101`；③ 401 + `40102`；④ 200 + `code=0` 且 `data.token` 非空。<br>（详细判据与备注见 `TC-01-1.2-1/2/3` 与 `TC-01-1.4-1` —— 本条只确认**没有回归**，不重复那几条的推理链） | |
| TC-02-R-4 前端静态页与 `/api` 反代（nginx 改动后） | ① `curl -s -o /dev/null -w 'HTTP %{http_code}\n' http://localhost:8088/`<br>② `curl -s http://localhost:8088/ \| head -c 200`<br>③ `curl -s http://localhost:8088/api/health` | ① `HTTP 200`；② 输出是 SPA 的 `index.html` 开头（含 `<div id="app">`）；③ 与 R-1 逐字同构的 `Result`（说明加了 `proxy_buffering off` 之后 `/api/` 整段反代没坏 —— 这是设计 §6.5 的"待实测确认"那一项） | |

> **阶段总验收（设计 §8 最后一行）**：「选择『本地 Qwen』走 Ollama、选择『云端 DeepSeek』走 DeepSeek，均能实时打字机输出」
> = **TC-02-2.1-1 + TC-02-2.1-3 + TC-02-2.4-1** 三条的组合实测（云端那半需要先做 §0.2）。
> **三条都过 = 阶段验收过**；前两条是协议侧、第三条是界面侧 —— 少任何一条都只算"跑通了一半"。

## 6. 契约（只能静态复核）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-02-C-1 契约文档先落、且四种帧齐备 | ① `cd /mnt/c/wp/nexus-agent-workbench && grep -n 'ChatStreamMeta\|ChatStreamDelta\|ChatStreamDone\|ChatStreamError' docs/api/openapi.yaml`<br>② `cd /mnt/c/wp/nexus-agent-workbench && sed -n '/^### 6\.2/,/^### 6\.3/p' docs/api/README.md \| head -40`<br>③ `cd /mnt/c/wp/nexus-agent-workbench && grep -n '^### 6\.3\|^### 6\.4\|^### 6\.5' docs/api/README.md` | ① 四份 schema 都在（每份至少有 `Xxx:` 定义与引用两处）；<br>② §6.2 的帧表里四种帧名（`meta`/`delta`/`done`/`error`）与 `data` 字段清单齐备，且**与 ① 的四份 schema 逐项一致**（两份对应关系在 README §6.2 与 openapi 的接口描述里各写一份）；<br>③ §6.3 失败形态表、§6.4 三个错误码（`40001`/`10200`/`20100`）、§6.5 curl 验证三段都在 | |
| TC-02-C-1 备注 | **为什么只能静态复核**：这几条是"文档对不对"，没有运行期载体 —— 契约的价值恰恰在于它**先于**代码存在（`docs/design/02` §6.7）。`grep` 只证"在不在"，"逐项一致"那半由人工核对（帧表加一行就要问一次"机器可读版里有没有对应 schema"）。 | 见上 | |

## 7. 执行前已知的偏差 / 噪音（**都不是本文件的判据**）

留着它们是为了让执行者**不要把它们读成"阶段2 的缺陷"**，也**不要**为了对齐文档去改代码或改契约
（那分别是 backend-engineer / devops-engineer 的活；发现即上报）。

| # | 现象 | 证据（2026-09-20，只读探针） | 影响 |
|---|---|---|---|
| 1 | **缺 `Content-Type` 时不是契约写的 `415`，而是 `500` + `50000`** | `curl -s -w 'HTTP %{http_code}' -X POST localhost:8089/api/auth/login -H 'Content-Type: text/plain' -d '{…}'` → `HTTP 500` + `{"code":50000,"msg":"系统繁忙，请稍后重试","data":null}`；不带 `Content-Type`（curl 默认 `application/x-www-form-urlencoded`）同形。机制：`HttpMediaTypeNotSupportedException` 被 `GlobalExceptionHandler` 的 `@ExceptionHandler(Exception.class)` 兜底收走（那里返回 500），**没有**专门的 415 出口 | `docs/api/README.md` §6.1 与设计 §5.1-2 都写"415 且响应体不是 Result"。实际形态仍是**一个合法的 `Result`**（前端 `isResult` 为真）⇒ 页面会显示 `系统繁忙，请稍后重试`，**排查方向被带偏**（正是那段文字想避免的事）。见 **TC-02-2.4-6** 的备注：判据只认"首请求成功"，实际形态按实测记录 |
| 2 | `docker compose` 每次都打 `The "model" variable is not set` 警告 | `docker compose ps` 即可复现 | **阶段0 遗留**（`docker-compose.yml:111` 的注释里有个未转义的 `${model}`，在 YAML 块标量里被 compose 当变量插值）。纯噪音，不影响任何行为；**不属阶段2** |
| 3 | 设计 §6.4 的密钥验证片段用的是**裸 `up -d`**；而"只改了 `env_file` 指向的文件**内容**"时，`up -d` **可能**是空操作（**未实测**） | 本文件因此把 §0.2 第 ② 条改写为 `up -d --force-recreate nexus-backend`（口诀：拿不准就 `--force-recreate`） | 若 `up -d` 真没重建容器，第 ③ 步的 `printenv \| wc -c` 会打出 `0` —— **判据落在那一行**，不在命令本身；见到 0 就重跑第 ② 条。另：`env_file.required: false` 带来的"静默创建无密钥容器"，设计 §6.0 第 0.5 步已记过 |
| 4 | **SSE 响应头的 `charset`：已拍板「不加」（2026-09-21）** | **实测值**（2026-09-21）响应头原文：`Content-Type: text/event-stream`（**裸值，没有** charset 参数；同一份捕获里相邻三行是 `Transfer-Encoding: chunked` / `Date: …` / 空行）。**判断依据**：SSE 的 IANA 注册里 `charset` 只是"为兼容遗留服务端"保留的**可选**参数、事件流恒为 UTF-8 ⇒ 本后端**刻意不加**，与**契约 §6.1**（裸 `text/event-stream`）一致，**实现正确**；错的是那句**措辞**（它照抄了下述两处**理由写错**的文档）。持该说法（原文）的两处：`docs/api/README.md` 里那句"Spring 会给 `text/event-stream` 带上 `;charset=UTF-8`，全等比较会误判"与 `docs/design/02-统一AI网关.md` 的同句 | **对代码无影响**：前端判响应类型用的是 `includes('application/json')`（与 charset 无关）。受影响的只有**本文档判据 ① 的措辞**（原稿照抄了 `;charset=UTF-8`）—— **已订正为裸 `text/event-stream`**。那两处**文档**的措辞已由 `backend-engineer` **在同一批订正完毕** —— 主会话复核：`README` §6.3 / 设计 §5.1-2 / **`openapi.yaml` 两处**（其中一处原文是**直接断言成功响应头带 charset**，与实测正面冲突，最重），**共 4 处**。另：`docs/agent-log/20260918-阶段2设计评审-前端视角.md` 里的同款说法**按「不改历史留档」原样保留**（日期化记录，改它等于篡改；发现即上报）。执行者仍不自行改契约/设计文档 |

---

## 附：测不出来的项与去向

本文件**不另设附表**（2026-09-18 起废弃集中汇总，见 `TC-01.md` 的同类改动）。每一项「测不出来」都写在
**它所属用例的备注行**里，就地下判断；另有四处需要外部落点的，指向如下：

| 项 | 为什么测不了 / 落点在哪 |
|---|---|
| **上游是否真的停止生成**（2.3-2 的"且上游不再继续"） | Ollama 侧有没有可判读的取消痕迹，本项目**未留档**；主判据是后端那条"客户端断开，已取消上游调用"日志（它证明 `InputStream` 被关）。见 **TC-02-2.3-3** 的预期 ⑤ 与备注 |
| **软上限截断**（`done` + `finishReason=timeout`，290s） | 要等 ≥ 290 秒才自然发生；本文件**不列为用例**。若要看它，等价做法是把 `nexus.ai.soft-timeout-ms` 压到几秒（改 `application.yml` → 重打包 + 重启 = 分钟级），**代价高于收益**；其逻辑已由后端单测覆盖（`ChatServiceImplTest` 的软上限定时器用例） |
| **`nexus-backend` 的 `20100` 与上游中途断开**（流进行到一半上游报错） | 要造它得让上游在**流中途**失败（本项目没有可编排的上游桩）。当前环境的"上游不可用"只能造"开流前就失败"那一半，见 **TC-02-2.3-4** |
| **前端在 Vite dev（5173）下的 SSE 行为** | 设计 §6.5 已约定：SSE 的验收与排查**一律走 8088**，5173 那条链路本轮未验。见 **TC-02-2.3-2** 的备注 |
