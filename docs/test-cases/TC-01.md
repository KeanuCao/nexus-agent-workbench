# TC-01 多租户与认证（阶段1）

> 对应任务：[`docs/task/task.2+多租户认证.md`](../task/task.2+多租户认证.md) ｜ 设计：[`docs/design/01-多租户与认证.md`](../design/01-多租户与认证.md)
> 接口契约：[`docs/api/README.md`](../api/README.md) §1 / §5 —— **以它为准，不从后端代码反推**
>
> **记录纪律（与 task.2 的「已实现 ≠ 已验收」同一口径）**：
> 「实测」栏**留空即表示未测**，未实测不得填「通过」。失败时把实际观察**原样**记下来（含错误文案），
> 不要写「已修复」——修复后重跑并记录新的结果。
>
> **可执行纪律（2026-09-15 新增，硬要求）**：「步骤」栏必须**可被另一个人无损复现**。
> **命令类**：每格是能直接复制粘贴执行的完整命令，含必需的 `cd <目录>` 前缀。
> **操作类**（§4 前端交互这类）：写成逐步编号动作（点哪个按钮、输入什么、每步立刻能看到什么），同样禁止压缩。
> 一律禁止把多步压成一行箭头串 —— 多步就分行。
> 判据：照着这一格走一遍，不需要任何补充信息、不需要任何自行翻译。（来历见 [`TC-00.md`](TC-00.md) 头部。）
>
> **编号对照（2026-09-17 补）**：本文件的 `TC-01-X.Y-N` 是「**用例序号**」（同小节内第 N 条用例）；
> 而 [`design/01`](../design/01-多租户与认证.md) §9、[`task.2`](../task/task.2+多租户认证.md) 的 `X.Y-N`
> 是「task 原文的**第 N 条验收标准**」—— **两套编号不是一回事**：有两处会指到不同的东西，另有一处是本文件独有的新增用例（第三条）：
> - `1.2-3`：design 指「登出即失效」= 本文件的 **TC-01-1.2-5**（本文件 1.2-3 是「过期 token」—— 因 task 的第 1 条被拆成 1.2-1/2/3 三条用例）；
> - `1.4-3`：design 指「返回的 Token 可访问受保护接口」= 本文件的 **TC-01-1.4-6**（本文件 1.4-3 是「用户不存在」）；
> - `1.3-5`（2026-09-17 新增）：**本文件独有的用例**，task / design 里没有对应条目 ——
>   它补的是 task.2 `1.3-2`（「两个租户的数据互不可见」）里 **TC-01-1.3-2 覆盖不到的那半边**（见该行下方的备注）。
>
> ⚠️ 2026-09-18 起还有**第三种情形**：design/task 的 `1.3-3`（「`@IgnoreTenant` 场景可正确跳过」）在本文件
> **已无对应用例** —— 原 TC-01-1.3-3 已评估移除（豁免**生效**由 **TC-01-1.4-1** 守 —— fail-closed 下豁免若失效、
> 登录那条查询会抛异常变 500；**多加**豁免由 **TC-01-1.3-5** 守；余下只剩断言日志文案，代价高于收益）。
> 该验收项的运行期落点即 **TC-01-1.4-1**。
>
> 引用时请**带 `TC-01-` 前缀**或直接写用例名，别只写 `1.2-3`。
>
> **脚本化（2026-09-17）**：`1.2-4` / `1.2-5` / `1.3-2` / `1.3-5` 四条已落成 `qa/scripts/tc01-*.sh`
> —— 步骤栏只留一行可复制粘贴的命令 + 一句「这一步在干什么」（编写纪律 §1）。
> 四条脚本共用一个**只装机械动作**的库 `qa/scripts/lib/tc-common.sh`（路径与端口解析 / JSON 取值 /
> HTTP 动作 / Redis 指纹 / 退出路径的清理脚手架）；**判据一律留在本文档**，脚本只做动作、只打印观察，
> 不打 PASS/FAIL，退出码只表示「动作做没做成」。四条都会写 Redis 白名单键，都自带幂等清理（含
> Ctrl-C / kill 的 trap 兜底）与跑前/跑后**键清单**指纹自证。
>
> **【脚本形态待跑】标注（2026-09-18 加）**：表里带 **【脚本形态待跑】** 的用例，指它的**步骤栏已经是脚本形态**，
> 但**脚本形态尚未对着真后端跑过**（至多做过了 `--self-test` 与桩环境干跑；本文件没有它们跑过真后端的记录）。
> ⚠️ **两种形态不是同一件事，别混**：
> - `TC-01-1.2-4` / `TC-01-1.2-5`：**旧手工形态用户已手工跑过且通过**（2026-09-18 用户确认），「实测」栏已按**手工形态**记录；
>   **脚本形态仍未跑**，故标注保留 —— 手工形态过了**不能**替脚本形态结案。
> - `TC-01-1.3-5`：**脚本形态已跑且通过**（2026-09-18 用户确认），标注已撤销。
> - `TC-01-1.3-2`：**两种形态都没有「已跑过且有结果记录」** —— 标注保留、「实测」栏留空。
> - 回填「实测」栏时**必须注明形态**（手工 / 脚本）—— 这是本节唯一的形式要求。
>
> （`TC-01-1.2-3` 用的 `tc01-expired-token.sh` 是**纯本地计算夹具**、没有多步机械动作，不在此标注范围。）

## 0. 前置条件

| 项 | 值 |
|---|---|
| 环境 | `./scripts/up.sh` 已跑通，后端 `/api/health` 三项 UP |
| 迁移 | `t_db_patch` 有 2 行；`t_tenant` 2 行、`t_user` 2 行 |
| 种子账号 | `admin` / `admin123`（租户 `default`）、`demo` / `demo123`（租户 `demo`）|
| 端口 | 后端 `8089`、前端 `8088`（取自 `docker-compose/.env`）|
| 日志 | `docker logs -f nexus-backend` 开着（多处判据要看 SQL 与日志）。TC-01-1.2-5 的脚本自己会抓一段 `docker logs --tail 200 nexus-backend` 做佐证（**取不到不影响判据**，该段会说明取不到的原因）|
| Redis 指纹 | WSL 内能跑 `docker exec nexus-redis redis-cli`（TC-01-1.2-4 / 1.2-5 / 1.3-2 / 1.3-5 四个脚本用它做无害性自证；取不到时脚本以**退出码 3** 停下，不会带着"无法自证"继续写键）|

> `curl` 统一用 `-s`（**不加 `-f`**）—— 401/503 时也要拿到响应体才能核对 `code`。

---

## 1. JWT 认证过滤器（1.2）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.2-1 无 token | `curl -s -w '\n%{http_code}' localhost:8089/api/auth/me` | HTTP **401**；body 是 `Result`，`code=40100`，`data=null` | |
| TC-01-1.2-2 伪造 token | 上面加 `-H 'Authorization: Bearer forged.token.value'` | HTTP **401**，`code=40101`（**不是** 40102） | |
| TC-01-1.2-3 过期 token | ① `TOKEN=$(bash /mnt/c/wp/nexus-agent-workbench/qa/scripts/tc01-expired-token.sh) && echo "token 长度=${#TOKEN}"` —— 在本地签一个「签名正确、`exp` 已过期 60 秒、`jti` 从未进过 Redis」的 token（夹具**只做本地计算**，不写库 / 不写 Redis / 不改配置 / 不碰容器，stderr 会打印密钥来源与 iat/exp 供核对）<br>② `curl -s -w '\n%{http_code}' -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/me`<br>（两步要在**同一个 shell** 里跑，② 用 ① 存下的 `$TOKEN`） | HTTP **401**；body 是 `Result`，`code=40102`（**不是** 40101） | |
| TC-01-1.2-3 备注 | ⚠️ **为什么不用「把有效期改小」的路线**：①「改成 `0`」根本走不通 —— `TokenStore.save` 有 TTL 正数守卫（`ttlSeconds <= 0` 抛 `SystemException`），`nexus.jwt.expire-seconds: 0` 会让**登录直接 500**（`code=50000`），连 token 都拿不到，步骤 ① 无法完成（该守卫是有意的，不是缺陷）；②「改成小正数（如 `2`）再登录 → 睡 3 秒」要**重建 `nexus-backend` 容器**，而 `docker-compose.yml` 的 `nexus-backend.environment` 里**没有** `NEXUS_JWT_EXPIRE_SECONDS`（得先改 compose 加一行），生效期间**其他用例**登录拿到的 token 也会秒过期 —— 属污染，与 Test Harmlessness 冲突。<br>**等价性**：过滤器的判定顺序是「② 解析（含有效期）→ ③ Redis 白名单」，② 在 ③ 之前，所以「本地签一个已过期的」与「真签发后等到过期」走的是**同一条路径**；而「配置 → token 有效期」那一段由 **TC-01-1.4-1 的 `expiresIn=7200`** 覆盖。<br>**拿到 40101 而不是 40102 时**：多半是夹具密钥与后端不一致（签名错），先核对 ① 的 stderr 里那行「密钥来源 / 密钥指纹」，别当成产品缺陷。<br>夹具在宿主 `qa/scripts/` 下，在 WSL 里跑，**不需要 push**（不进 builder 容器，与 TC-00 §2 的情形不同）。 | 见上 | |
| TC-01-1.2-4 合法 token 放行 【脚本形态待跑】 | ① `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc01-login-then-me.sh` —— 脚本做的事：真登录 `admin`（写 1 个 Redis 白名单键）→ 打印**登录响应原样**（含 `data.user`）→ 用**同一个** token 调 `GET /api/auth/me` → 打印响应原样 → 把「登录响应的 `data.user`」与「`/me` 的 `data`」**逐字段并排**打印（6 个字段，同 §5.2 的 `UserInfoVO`；**比对由人眼做**）→ 登出该 token → 打印 Redis 白名单的跑前/跑后指纹与差异。<br>退出码只表示「动作做没做成」（0=完成；3=环境前提不成立；4=登录没拿到 `data.token`）—— **结论看它打印的原始响应，不是看退出码** | ① 登录：HTTP **200** + `code=0`，`data.token` 非空；<br>② `GET /api/auth/me`：HTTP **200** + `code=0`；<br>③ 字段对照表里 `userId`/`username`/`nickname`/`tenantId`/`tenantCode`/`tenantName` **两列逐字段一致**；表里一旦出现「（取不到）」就是**没取到值**，**不是"两边一致"**；<br>④ 指纹：「跑后有、跑前没有的键」为空，自建 jti 的 `EXISTS` 为 `0` | ✅ **通过**（2026-09-18 用户手工验收，**手工形态**；观察未逐项留档）<br>⚠️ **脚本形态尚未跑** —— 见上「【脚本形态待跑】标注」段 |
| TC-01-1.2-4 备注 | ⚠️ **本条的 PASS 不要读大了**：它证的是「合法 token 能过（过滤器 ② 签名 + ③ 白名单都放行）」，**不能**证「该接口需要鉴权」—— 那半边由 **1.2-1**（无 token → 401）与 **1.2-2**（伪造 token → 401）覆盖；也不能证签名错/过期会怎样（那是 1.2-2 / 1.2-3）。<br>**为什么脚本化**：原步骤要执行者手工 `TOKEN=$(curl … \| grep -o '"token":"[^"]*"' \| cut -d'"' -f4)` 再把 `$TOKEN` 喂给第二条（WSL 内无 `jq`，只能这么拼），而拼接错误会被误读成产品缺陷 —— 机械动作归脚本，**比对留给人**。<br>**头号假通过形态**：取值器坏掉 → 两边都取不到值 → 表格两列全空、看着反而"完全一致"。堵它的两条：取不到就打印「（取不到）」（不留空）、`--self-test` 里专门验「取不到的路径必须失败而不是静默留空」。<br>（**脚本形态的真后端路径未实测**：`--self-test`（纯本地取值器自检）与桩环境干跑已过；**脚本形态**对着真后端那一遍尚未跑。手工形态已跑并记录于「实测」栏，但那**不能**替脚本形态结案。） | 见上 | |
| TC-01-1.2-5 **登出即失效** 【脚本形态待跑】 | ① `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc01-logout-then-me.sh` —— 脚本做的事：真登录 `admin`（写 1 个 Redis 白名单键，读出该 `jti`，打印 `EXISTS=1`）→ **登出前**先用这个 token 调一次 `/me`（基线）→ `POST /api/auth/logout`（同一个 token）→ 再读一次该 `jti` 的 `EXISTS` → 用**同一个 token** 再调 `/me` → 打印后端日志里「含本次 `jti` 的行」与「`token 非法` / `token 已过期` 的行」（原样，做 ③/② 分流佐证）→ 幂等重登出（键已不在 ⇒ 后端回 401，**属预期**）+ Redis 指纹。<br>退出码只表示「动作做没做成」（0=完成；3=环境前提不成立；4=登录没拿到 `data.token`）—— **结论看它打印的原始响应** | ① 登出前的 `/me`：HTTP **200** + `code=0`（基线：这个 token 在登出前确实有效）；<br>② `POST /api/auth/logout`：HTTP **200** + `code=0`、`data=null`；<br>③ 登出后该 `jti` 的 `EXISTS` = **0**；<br>④ 用**同一个 token** 再调 `/me`：HTTP **401** + `code=40101`，且**能看出走的是过滤器第 ③ 步**（`40101` 是两个原因共用的码，见下一行备注 —— 拿到 40101 ≠ 本条通过）；<br>⑤ 佐证：日志里出现「**Redis 白名单无此登录态（已登出或被清除）**」+ `path=/api/auth/me` + **本次 `jti`**，且第 (b) 段里**没有**指向本次请求的「`token 非法（type=…）`」；<br>⑥ 指纹：「跑后有、跑前没有的键」为空，自建 jti 的 `EXISTS` 为 `0` | ✅ **通过**（2026-09-18 用户手工验收，**手工形态**；观察未逐项留档）<br>⚠️ **脚本形态尚未跑** —— 见上「【脚本形态待跑】标注」段 |
| TC-01-1.2-5 备注 | ⚠️ **本条的判据是「登出之后落在第 ③ 步」，不是「拿到 `40101` 就算过」**：`40101` 是**两个原因共用的码** —— 过滤器第 ③ 步「Redis 白名单里没有该 jti（已登出/被清除）」用它，第 ② 步「token 非法（签名不匹配 / 格式非法）」**也用它**，两者都是 HTTP 401 + `code=40101`。<br>**怎么把两条路径分开**（脚本第 ⑥ 段）：第 ③ 步的 warn **带 jti**、文案是「鉴权失败：Redis 白名单无此登录态（已登出或被清除），path=… jti=…」；第 ② 步的两条 warn **不带 jti**、文案是「鉴权失败：token 非法（type=…）」/「…token 已过期」。⇒ 日志里能与**本次 jti** 对上的那一行，只可能来自 ③。（文案取自 `JwtAuthenticationFilter` 的 134 / 144 / 148 / 180 行；哪天这两处日志改了，要同步复核本行与脚本 `--self-test` 里的样例。）<br>**日志取不到时的推理链兜底**：第 ② 步只看「token 字节 + 当前时刻」，而第 ④ 步的登出回了 200 ⇒ 那一刻 ② 与 ③ 都过了；token 一字未变 ⇒ 第 ⑤ 步的 `40101` 只可能来自 ③。<br>**别踩的坑**：⑤ 若拿到 **`40102`** → 那是第 ② 步（过期），与登出无关；若拿到 **200** → 登出根本没生效（先看 ③ 的 `EXISTS` 是不是 1）。<br>**为什么加了「登出前先调一次 /me」这个基线动作**（2026-09-17 新增）：没有它，「登出后 401」与「这个 token 本来就不行」分不开；顺带堵住"logout 根本没要求鉴权、回 200 却什么也没做"这种假通过。<br>（**脚本形态的真后端路径未实测**：`--self-test` 与桩环境干跑已过；**脚本形态**对着真后端那一遍尚未跑（手工形态见「实测」栏）。其中 **⑤ 的佐证段**尤其未实测 —— 那两行 warn 的文案是按源码核对的，运行期是否真出现在 `docker logs` 窗口里、格式如何，要跑一次才知道；脚本取不到时会自己说明原因，不影响 ③④⑤ 的原始响应。跑完把观察回填「实测」栏。）<br>（**可选**：若你想自己再抓一遍日志 —— `docker logs --tail 200 nexus-backend 2>&1 \| grep -F '<脚本第 ② 段打印的那个 jti>'`，把 `<…>` 换成实际 jti。这不是步骤，只是脚本第 ⑥ 段之外的复核手段。） | 见上 | |
| TC-01-1.2-6 白名单放行 | 不带头调 `/api/health` 与 `/actuator/health` | 均**不需要** token（200 / 200 或 503）—— 加了鉴权会让 0.3 的三个脚本全线 FAIL | |

## 2. 多租户隔离（1.3）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.3-1 **SQL 自动带 tenant_id** | `docker logs nexus-backend` 里找 `/api/auth/me` 触发的查询 | SQL 形如 `... WHERE ... tenant_id = ?`，参数为当前租户；**这是本项验收的判据，必须肉眼看到** | |
| TC-01-1.3-2 两租户各绑自己的上下文 【脚本形态待跑】 | ① `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc01-two-tenant-me.sh` —— 脚本做的事：真登录 `admin` 与 `demo`（各写 1 个 Redis 白名单键）→ 用两个 token **各**调一次 `GET /api/auth/me` → 把两次响应体**原样**并排打印（含 HTTP 状态码、登录响应里的 `data.user`、`/me` 的 `data`、本地解码出的 token 载荷）→ 登出这两个 token → 打印 Redis 白名单的跑前/跑后指纹与差异。<br>退出码只表示「动作做没做成」（0=完成；3=环境前提不成立；4=登录没拿到 token）—— **结论看它打印的原始响应，不是看退出码** | ① 两条 `/me` 均 **HTTP 200 + `code=0`**；<br>② 每条 `data` 的 `userId`/`username`/`nickname`/`tenantId`/`tenantCode`/`tenantName` 与**同一次登录**返回的 `data.user` **逐字段一致**；<br>③ 两账号的 `tenantId`/`tenantCode` 互不相同（`default` vs `demo`）—— 即「上下文确实按各自 token 绑定」；<br>④ 指纹：**「跑后有、跑前没有的键」为空**，两个自建 `jti` 的 `EXISTS` 均为 `0`（「跑前有、跑后没有」若非空，只可能是既有键在窗口内自然过期，脚本会列出是哪几个） | |
| TC-01-1.3-2 备注 | ⚠️ **本条证明不了「够不到别人的数据」**（2026-09-17 复核确认）：`GET /api/auth/me` **不收任何入参**（`AuthController.java:84-87`），永远只回调用者自己 —— 两次返回值基本上就是**各自 token 里的 claim**（脚本把解码后的载荷一并打印，正是为了让这一点肉眼可见）。所以即便租户条件**完全没被注入**，这两次调用照样各回各的、一字不差（**假阴性**）。<br>本条的**真实价值**是抓「租户上下文取到了**错值**」（ThreadLocal 串租户 / tenantId 绑成常量或别人的值）：那时 `selectById` 会带上错条件 → 查不到该 userId → 401，而不是"照常返回自己"。<br>「够不到」那半边的真判据在 **TC-01-1.3-5** 与 **TC-01-1.3-1**。 | 见上 | |
| TC-01-1.3-5 **跨租户 token 必被拒**（运行期 fail-closed） | ① `cd /mnt/c/wp/nexus-agent-workbench && bash qa/scripts/tc01-cross-tenant-token.sh` —— 脚本做的事：真登录 `admin`、`demo` → **先用本地密钥验签那份真 token**（验不过＝夹具密钥与后端不一致 ⇒ 退出码 3，**不给结论**）→ 本地用同一密钥重签两份 token（**逐字节沿用真 token 的 header，沿用它的 `jti`/`sub`/`iat`/`exp`，只把 `tenantId` 换成对方租户的**）→ 两个方向各调一次 `GET /api/auth/me` 并打印原始响应 → 登出两个真 token → 打印 Redis 指纹与差异。<br>**脚本不写任何新状态**：两份伪造 token 复用真 token 的 `jti`（因而过得了过滤器第 ③ 步），Redis 里只会多出两次真登录写的键 —— 作用域与 TC-01-1.3-2 完全相同。<br>② （可选佐证）`docker logs --tail 30 nexus-backend 2>&1 \| grep -F '取当前用户失败'` | ① **方向 A**（`sub`=admin 的 userId、`tenantId`=demo 的租户）：HTTP **401** + `code=40101`；<br>② **方向 B**（`sub`=demo 的 userId、`tenantId`=admin 的租户）：HTTP **401** + `code=40101`；<br>③ 指纹：「跑后有、跑前没有的键」为空，两个自建 `jti` 的 `EXISTS` 均为 `0`。<br>**若出现 HTTP 200**：看 `data` —— 会是「A 租户的 `username`/`userId` + B 租户的 `tenantId`/`tenantCode`/`tenantName`」的**混合身份**，即租户条件根本没进 SQL ⇒ **跨租户泄漏，FAIL**（同时去核 TC-01-1.3-1 的 SQL 日志）。<br>**除 401 + `40101` 之外的任何结果**（200 / 500 / 其它 `code`）都照实记录并上报，别硬解释 | ✅ **通过**（2026-09-18 用户手工验收，**脚本形态**；观察未逐项留档）<br>⚠️ 佐证行（步骤 ②）与 1.3-5 备注末句的「未实测」标注**未撤销** —— 用户未单独确认该行，不予推定 |
| TC-01-1.3-5 备注 | ⚠️ **40101 不等于「隔离生效」** —— **签名错也是 40101**（同一个码）。密钥配错（后端被 `NEXUS_JWT_SECRET` 覆盖过）时，伪造 token 会被过滤器第 ② 步拒掉，**伪装成 PASS**。这正是脚本把「用本地密钥验签真 token」放在最前面的理由：验过 ⇒ python 的 HMAC-SHA256 与后端 jjwt 的签发逐字节一致 ⇒ 40101 才真正指向"租户隔离把它拒了"；验不过 ⇒ 退出码 3、不给结论。<br>**为什么只有重签 token 能造出这个形状**：`tenantId` 由过滤器从 token 取出后写入 `TenantContext`（`JwtAuthenticationFilter:158`），`t_user` 查询的 `tenant_id = ?` 由 `TenantLineHandlerImpl` 从上下文取 —— 没有任何接口能改这两处，唯一入口就是 token 本身。而这不是"取巧造负路径"：`docs/api/README.md` §5.4 末句**白纸黑字承诺过**这个行为，本用例就是它的运行期判据。<br>**为什么要两个方向**：两个账号的真实 `tenantId` 不同（1 / 2），两个方向才覆盖到"错配值恰好是另一个真实租户"这一具体形状；只做一个方向，将来若只剩一个租户会假通过。<br>**与「没有租户上下文」那一支的分工**（原写的是「与 TC-01-1.3-4 的分工」，该行已于 2026-09-18 移除：ut 的内容不放进 TC 表）：那一支仍不做运行期用例（要故意写一条漏标注解的查询并重新部署，成本高于收益）；本条管「上下文**有、但与身份不符**」时的 fail-closed —— 它之所以能搬进运行期，是因为注入点既不是配置、也不是容器，而是一份**纯本地计算出的 token**（零写库、零改配置、零重建）。<br>（佐证行未实测：`docker logs` 里是否真有那行 warn 取决于日志格式与窗口，实测后回填。） | 见上 | |

> （**本节的空号 `1.3-3` / `1.3-4`**：原两条用例已于 **2026-09-18** 移除 —— 编号**保留空号、不重排**，以免历史引用
> （agent-log / 记忆 / commit）失锚。两条都是**评估后移除**、不是"漏了"：`1.3-3` 的理由见上方「编号对照」末段；
> `1.3-4` 是 ut / 设计内容，按用户意见不放进 TC 表 —— 其「没有租户上下文」那一支的实际口径写在 `1.3-5` 备注里。）

## 3. 登录接口（1.4）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.4-1 正常登录 | `curl -s -X POST localhost:8089/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}'` | HTTP 200，`code=0`，`data.token` 非空、`tokenType=Bearer`、`expiresIn=7200`（= `application.yml` 的 `nexus.jwt.expire-seconds` 默认值；TC-01-1.2-3 不改该配置，故两格互相自洽）、`data.user.tenantName=默认租户` | |
| TC-01-1.4-2 密码错 | 同上，密码改 `wrong` | HTTP **200**（业务失败走 200），`code=10100`，`msg` 可展示；**响应体无堆栈** | |
| TC-01-1.4-3 用户不存在 | 用户名改 `nosuchuser` | **与 1.4-2 完全同码同文案**（防账号枚举）—— 两条的 `msg` 必须一字不差 | |
| TC-01-1.4-4 账号停用 | `UPDATE t_user SET status=0 WHERE username='demo'` 后登录 `demo` | `code=10101`；**测完把 status 改回 1** | |
| TC-01-1.4-5 租户停用 | `UPDATE t_tenant SET status=0 WHERE tenant_code='demo'` 后登录 `demo` | `code=10102`；**测完改回 1** | |
| TC-01-1.4-6 token 可访问受保护接口 | 拿 1.4-1 的 token 调 `/api/auth/me` | 200 + `code=0` | |

## 4. 前端交互（1.5）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.5-1 登录成功进控制台 | 浏览器开 `http://localhost:8088`，用 `admin/admin123` 登录 | 落到 `/dashboard`；导航栏显示用户名与租户名 | |
| TC-01-1.5-2 无 token 跳登录 | ① 浏览器打开 `http://localhost:8088`，按 F12 → **Application → Local Storage → `http://localhost:8088`**<br>② 删除其中的 token 项（或点 Clear all）<br>③ 地址栏直接输入 `http://localhost:8088/dashboard` 回车 | 跳 `/login?redirect=/dashboard`；登录后**回到 `/dashboard`** 而非 `/` | |
| TC-01-1.5-3 401 自动登出 | ① 用 `admin/admin123` 登录，进入 `/dashboard`<br>② F12 → Application → Local Storage，把 token 的值改坏（末尾加一个字符）<br>③ **按 F5 刷新页面** —— 让 store 重新从 localStorage 读值（只改 localStorage、不刷新**不会**触发，见下方备注行）<br>④ 刷新后页面会自行发出受保护请求 | 自动回登录页且 token 被清 | |
| TC-01-1.5-3 备注 | ⚠️ **陷阱**：`/api/health` 是白名单接口、且 store 的内存值是权威 —— 只改 localStorage **不刷新**不会触发 | 见上 | |
| TC-01-1.5-4 已登录访问 /login | 登录状态下直接访问 `/login` | 直接回 `/dashboard`，不出现「登录了还停在登录页」 | |
| TC-01-1.5-5 退出登录 | 点导航栏「退出登录」 | 回登录页；**再点浏览器后退**仍进不去控制台（token 已从服务端失效） | |

## 5. 回归（不得破坏既有交付物）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-R-1 健康检查不受影响 | `curl -s localhost:8089/api/health` | 200 + `code=0` + 三项 `checks` 全 UP | |
| TC-01-R-2 巡检无新增失败 | `./scripts/check-health.sh` | FAIL 数不高于阶段0 基线（当时 `PASS 19 / WARN 1 / FAIL 0`） | |
| TC-01-R-3 版本号 | ① `cd /mnt/c/wp/nexus-agent-workbench && grep -o '<revision>[^<]*</revision>' backend/pom.xml`（取真源值）<br>② `curl -s localhost:8089/api/health \| grep -o '"version":"[^"]*"'` | ② 的版本号**等于** ①（证明资源过滤仍生效）。<br>⚠️ **不要**退化成「只判 semver 正则」—— 那会丢掉「改了版本号、产物却没跟着变」这条唯一能抓住版本漂移的判据（2026-09-15 实景：父 POM 0.2.1 / 子模块 0.2.0，构建成功且零警告，产物仍是 0.2.0） | |
