# TC-01 多租户与认证（阶段1）

> 对应任务：[`docs/task/task.2+多租户认证.md`](../task/task.2+多租户认证.md) ｜ 设计：[`docs/design/01-多租户与认证.md`](../design/01-多租户与认证.md)
> 接口契约：[`docs/api/README.md`](../api/README.md) §1 / §5 —— **以它为准，不从后端代码反推**
>
> **记录纪律（与 task.2 的「已实现 ≠ 已验收」同一口径）**：
> 「实测」栏**留空即表示未测**，未实测不得填「通过」。失败时把实际观察**原样**记下来（含错误文案），
> 不要写「已修复」——修复后重跑并记录新的结果。
>
> **可执行纪律（2026-09-15 新增，硬要求）**：「步骤」栏必须**可被另一个人无损复现**。
> **命令类**：每格是能直接复制粘贴执行的完整命令，含必需的 `cd <目录>` 前缀。
> **操作类**（§4 前端交互这类）：写成逐步编号动作（点哪个按钮、输入什么、每步立刻能看到什么），同样禁止压缩。
> 一律禁止把多步压成一行箭头串 —— 多步就分行。
> 判据：照着这一格走一遍，不需要任何补充信息、不需要任何自行翻译。（来历见 [`TC-00.md`](TC-00.md) 头部。）
>
> **编号对照（2026-09-17 补）**：本文件的 `TC-01-X.Y-N` 是「**用例序号**」（同小节内第 N 条用例）；
> 而 [`design/01`](../design/01-多租户与认证.md) §9、[`task.2`](../task/task.2+多租户认证.md) 的 `X.Y-N`
> 是「task 原文的**第 N 条验收标准**」—— **两套编号不是一回事**，有两处会指到不同的东西：
> - `1.2-3`：design 指「登出即失效」= 本文件的 **TC-01-1.2-5**（本文件 1.2-3 是「过期 token」—— 因 task 的第 1 条被拆成 1.2-1/2/3 三条用例）；
> - `1.4-3`：design 指「返回的 Token 可访问受保护接口」= 本文件的 **TC-01-1.4-6**（本文件 1.4-3 是「用户不存在」）。
>
> 引用时请**带 `TC-01-` 前缀**或直接写用例名，别只写 `1.2-3`。

## 0. 前置条件

| 项 | 值 |
|---|---|
| 环境 | `./scripts/up.sh` 已跑通，后端 `/api/health` 三项 UP |
| 迁移 | `t_db_patch` 有 2 行；`t_tenant` 2 行、`t_user` 2 行 |
| 种子账号 | `admin` / `admin123`（租户 `default`）、`demo` / `demo123`（租户 `demo`）|
| 端口 | 后端 `8089`、前端 `8088`（取自 `docker-compose/.env`）|
| 日志 | `docker logs -f nexus-backend` 开着（多处判据要看 SQL 与日志）|

> `curl` 统一用 `-s`（**不加 `-f`**）—— 401/503 时也要拿到响应体才能核对 `code`。

---

## 1. JWT 认证过滤器（1.2）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.2-1 无 token | `curl -s -w '\n%{http_code}' localhost:8089/api/auth/me` | HTTP **401**；body 是 `Result`，`code=40100`，`data=null` | |
| TC-01-1.2-2 伪造 token | 上面加 `-H 'Authorization: Bearer forged.token.value'` | HTTP **401**，`code=40101`（**不是** 40102） | |
| TC-01-1.2-3 过期 token | ① `TOKEN=$(bash /mnt/c/wp/nexus-agent-workbench/qa/scripts/tc01-expired-token.sh) && echo "token 长度=${#TOKEN}"` —— 在本地签一个「签名正确、`exp` 已过期 60 秒、`jti` 从未进过 Redis」的 token（夹具**只做本地计算**，不写库 / 不写 Redis / 不改配置 / 不碰容器，stderr 会打印密钥来源与 iat/exp 供核对）<br>② `curl -s -w '\n%{http_code}' -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/me`<br>（两步要在**同一个 shell** 里跑，② 用 ① 存下的 `$TOKEN`） | HTTP **401**；body 是 `Result`，`code=40102`（**不是** 40101） | |
| TC-01-1.2-3 备注 | ⚠️ **为什么不用「把有效期改小」的路线**：①「改成 `0`」根本走不通 —— `TokenStore.save` 有 TTL 正数守卫（`ttlSeconds <= 0` 抛 `SystemException`），`nexus.jwt.expire-seconds: 0` 会让**登录直接 500**（`code=50000`），连 token 都拿不到，步骤 ① 无法完成（该守卫是有意的，不是缺陷）；②「改成小正数（如 `2`）再登录 → 睡 3 秒」要**重建 `nexus-backend` 容器**，而 `docker-compose.yml` 的 `nexus-backend.environment` 里**没有** `NEXUS_JWT_EXPIRE_SECONDS`（得先改 compose 加一行），生效期间**其他用例**登录拿到的 token 也会秒过期 —— 属污染，与 Test Harmlessness 冲突。<br>**等价性**：过滤器的判定顺序是「② 解析（含有效期）→ ③ Redis 白名单」，② 在 ③ 之前，所以「本地签一个已过期的」与「真签发后等到过期」走的是**同一条路径**；而「配置 → token 有效期」那一段由 **TC-01-1.4-1 的 `expiresIn=7200`** 覆盖。<br>**拿到 40101 而不是 40102 时**：多半是夹具密钥与后端不一致（签名错），先核对 ① 的 stderr 里那行「密钥来源 / 密钥指纹」，别当成产品缺陷。<br>夹具在宿主 `qa/scripts/` 下，在 WSL 里跑，**不需要 push**（不进 builder 容器，与 TC-00 §2 的情形不同）。 | 见上 | |
| TC-01-1.2-4 合法 token 放行 | ① `TOKEN=$(curl -s -X POST localhost:8089/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' \| grep -o '"token":"[^"]*"' \| cut -d'"' -f4) && echo "token 长度=${#TOKEN}"`<br>② `curl -s -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/me`<br>（两步要在**同一个 shell** 里跑，② 用 ① 存下的 `$TOKEN`；WSL 内无 `jq`，故用 grep/cut 取值） | HTTP 200，`code=0`，`data.userId/tenantId` 与登录返回一致 | |
| TC-01-1.2-5 **登出即失效** | ① `TOKEN=$(curl -s -X POST localhost:8089/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' \| grep -o '"token":"[^"]*"' \| cut -d'"' -f4) && echo "token 长度=${#TOKEN}"`<br>② `curl -s -w '\n%{http_code}' -X POST -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/logout`<br>③ `curl -s -w '\n%{http_code}' -H "Authorization: Bearer $TOKEN" localhost:8089/api/auth/me`<br>（三步要在**同一个 shell** 里跑，②③ 用 ① 存下的 `$TOKEN`） | ② 200 + `code=0`；③ **必须 401 + `code=40101`**（白名单里已无该 `jti`，走的是 ③；与 TC-01-1.2-3 的 ② 不同码，正是两者的区别所在） | |
| TC-01-1.2-6 白名单放行 | 不带头调 `/api/health` 与 `/actuator/health` | 均**不需要** token（200 / 200 或 503）—— 加了鉴权会让 0.3 的三个脚本全线 FAIL | |

## 2. 多租户隔离（1.3）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.3-1 **SQL 自动带 tenant_id** | `docker logs nexus-backend` 里找 `/api/auth/me` 触发的查询 | SQL 形如 `... WHERE ... tenant_id = ?`，参数为当前租户；**这是本项验收的判据，必须肉眼看到** | |
| TC-01-1.3-2 两租户互不可见 | ① 用 `admin`（租户 1）的 token 调 `/api/auth/me`；② 换 `demo`（租户 2）的 token 复跑 | 两次返回各自的 `userId` / `tenantId`；**admin 永远拿不到 demo 的数据** | |
| TC-01-1.3-3 `@IgnoreTenant` 生效 | 登录时的 SQL 日志 | **不含** `tenant_id` 条件（登录时尚无租户上下文）；且日志有一行 `租户隔离豁免：...，原因：...` 的 warn 留痕 | |
| TC-01-1.3-4 fail-closed（设计自检） | 由单测覆盖（`TenantContextTest` / 逻辑推演），**不做**运行期用例 —— 造它需要故意写一条漏标注解的查询并重新部署 | 无此用例：记录为「由设计保证 + 单测覆盖」 | 不适用 |

## 3. 登录接口（1.4）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.4-1 正常登录 | `curl -s -X POST localhost:8089/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}'` | HTTP 200，`code=0`，`data.token` 非空、`tokenType=Bearer`、`expiresIn=7200`（= `application.yml` 的 `nexus.jwt.expire-seconds` 默认值；TC-01-1.2-3 不改该配置，故两格互相自洽）、`data.user.tenantName=默认租户` | |
| TC-01-1.4-2 密码错 | 同上，密码改 `wrong` | HTTP **200**（业务失败走 200），`code=10100`，`msg` 可展示；**响应体无堆栈** | |
| TC-01-1.4-3 用户不存在 | 用户名改 `nosuchuser` | **与 1.4-2 完全同码同文案**（防账号枚举）—— 两条的 `msg` 必须一字不差 | |
| TC-01-1.4-4 账号停用 | `UPDATE t_user SET status=0 WHERE username='demo'` 后登录 `demo` | `code=10101`；**测完把 status 改回 1** | |
| TC-01-1.4-5 租户停用 | `UPDATE t_tenant SET status=0 WHERE tenant_code='demo'` 后登录 `demo` | `code=10102`；**测完改回 1** | |
| TC-01-1.4-6 token 可访问受保护接口 | 拿 1.4-1 的 token 调 `/api/auth/me` | 200 + `code=0` | |

## 4. 前端交互（1.5）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-1.5-1 登录成功进控制台 | 浏览器开 `http://localhost:8088`，用 `admin/admin123` 登录 | 落到 `/dashboard`；导航栏显示用户名与租户名 | |
| TC-01-1.5-2 无 token 跳登录 | ① 浏览器打开 `http://localhost:8088`，按 F12 → **Application → Local Storage → `http://localhost:8088`**<br>② 删除其中的 token 项（或点 Clear all）<br>③ 地址栏直接输入 `http://localhost:8088/dashboard` 回车 | 跳 `/login?redirect=/dashboard`；登录后**回到 `/dashboard`** 而非 `/` | |
| TC-01-1.5-3 401 自动登出 | ① 用 `admin/admin123` 登录，进入 `/dashboard`<br>② F12 → Application → Local Storage，把 token 的值改坏（末尾加一个字符）<br>③ **按 F5 刷新页面** —— 让 store 重新从 localStorage 读值（只改 localStorage、不刷新**不会**触发，见下方备注行）<br>④ 刷新后页面会自行发出受保护请求 | 自动回登录页且 token 被清 | |
| TC-01-1.5-3 备注 | ⚠️ **陷阱**：`/api/health` 是白名单接口、且 store 的内存值是权威 —— 只改 localStorage **不刷新**不会触发 | 见上 | |
| TC-01-1.5-4 已登录访问 /login | 登录状态下直接访问 `/login` | 直接回 `/dashboard`，不出现「登录了还停在登录页」 | |
| TC-01-1.5-5 退出登录 | 点导航栏「退出登录」 | 回登录页；**再点浏览器后退**仍进不去控制台（token 已从服务端失效） | |

## 5. 回归（不得破坏既有交付物）

| 用例 | 步骤 | 预期 | 实测 |
|---|---|---|---|
| TC-01-R-1 健康检查不受影响 | `curl -s localhost:8089/api/health` | 200 + `code=0` + 三项 `checks` 全 UP | |
| TC-01-R-2 巡检无新增失败 | `./scripts/check-health.sh` | FAIL 数不高于阶段0 基线（当时 `PASS 19 / WARN 1 / FAIL 0`） | |
| TC-01-R-3 版本号 | ① `cd /mnt/c/wp/nexus-agent-workbench && grep -o '<revision>[^<]*</revision>' backend/pom.xml`（取真源值）<br>② `curl -s localhost:8089/api/health \| grep -o '"version":"[^"]*"'` | ② 的版本号**等于** ①（证明资源过滤仍生效）。<br>⚠️ **不要**退化成「只判 semver 正则」—— 那会丢掉「改了版本号、产物却没跟着变」这条唯一能抓住版本漂移的判据（2026-09-15 实景：父 POM 0.2.1 / 子模块 0.2.0，构建成功且零警告，产物仍是 0.2.0） | |

---

## 附：已知的「测不出来」与为什么

| 项 | 为什么不做运行期用例 |
|---|---|
| fail-closed 的实际触发 | 要故意写一条漏标 `@IgnoreTenant` 的查询并重新部署，成本高于收益；由单测 + 设计保证 |
| 401 防抖的真实并发时序 | 需要精确构造并发 401，前端无自动化测试框架（决策 D8），只做了时序推演 |
| Redis 不可用时的全员 401 | 会打断其他用例；需要时单独跑：`docker compose stop redis` → 任何受保护请求应 401 → `start redis` |
