# Step 10 收尾（固定 tag + 文档同步）

- **Agent**：devops-engineer ｜ **耗时**：约 6 分钟 ｜ **task-id**：a1e8ca75af1510f28

## 做了什么
- `OLLAMA_TAG` 固定为 `0.33.2`：manifest 实测与 latest 同 digest，`up -d ollama` 零下载重建，healthy，模型仍在卷内。
- 探测确认官方**无 CPU-only 更小 tag**（0.33.2-cpu / cpu-0.33.2 / latest-cpu 均 NOT-FOUND），CPU 项目认了 8.45GB fat 镜像，只报告未换 tag。
- 新建 `wsl.abc.md`（环境约定 + 四段式方法论 + 6 条实测踩坑记录 + 预案）。
- 同步设计文档 §2.1（healthcheck 与 tag）；勾选 task.1 的 0.1 完成状态；核对 `docs/核心任务.md` 无 0.1 单独标记故未动。

## 遇到的坑
- for 循环变量 `$t` 经双层 shell（Windows bash → `wsl -d ... -- bash -c`）展开为空——踩坑记录第 5 条的现场重演，拆成 3 条显式命令解决。

## 下次怎么改进
- 双层 shell 环境里**默认不用内层变量循环**，一律显式展开命令（教训已重复两次，必须固化）。
- tag 固定前先做 digest 比对：同 digest 则放心重建（零下载），不同则只改文件不动容器。
