# 起草 0.1 实机验证计划

- **Agent**：devops-engineer ｜ **耗时**：约 6 分钟 ｜ **task-id**：a45accdc58fb1095b

## 做了什么
- 基于 0.1 实际交付物与设计文档 §7 验收对照表，产出 Step 0-10 分步验证计划（含 10 个检查点、每步命令/预期/失败处理、成本耗时表）。
- 核心思路"先探测后下载"：manifest 探针（零下载）→ hello-world（KB 级）→ curl 探针（秒级），在花大流量前消除全部路径/健康检查风险。

## 遇到的坑
- 当时处于 plan mode，被要求只读；计划写入了 agent 自建的变体计划文件而非指定的计划文件，最终由主会话誊写到指定文件再走 ExitPlanMode。

## 下次怎么改进
- plan mode 下代理只能写指定 plan 文件；主会话应先告知代理"计划内容作为报告返回，由主会话落盘"。
- 验证计划里提前把"已知待实测风险"逐一映射到步骤（R1-R5），执行时不迷路——这个模式后续阶段照搬。
