1. 演示环境是Windows + WSL。这个应该是学习者自己需要搞定的事情。wsl参考.md里面有一些说明，你整理一下重新生成一个新的wsl.abc.md，强调一下ai时代文档不正确的时候要自己解决问题。
2. docker容器需要用国内的源，docker compose是第一步要生成的。所以在增加一个阶段就是环境准备和检查方法。
3. 前后端分别部署在不同的容器中，db也是单独的容器，redis/ollma也部署到容器里面。
4. 数据库需要有一个单独的目录来管理db-patch。命名规则用年月日时分来表示不同阶段的db-patch。数据库应该有一张表表示打了哪些db patch。如果重复打，需要报错。请设计一个合理的打db patch的工作流。
5. 专门有一个容器负责打包，前后端打包是否需要分开请酌情考虑。尽量节约资源。
6. 每个阶段性的任务完成之后，必须要生成测试案例。同时思考如何在演进过程中如何同步测试案例。