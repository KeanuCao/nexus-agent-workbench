# [AI发现] 文档里写的测试用例数与实际条数不符

- **发现时间**：2026-09-15 ｜ **发现场景**：用户报 TC-00-0.1-4 通过、要更新测试台账时逐行点数 ｜ **状态**：待定（用户 2026-09-15 决定放入草稿，暂不订正）

## 现象

两处文档写了用例数，与文件里的实际条数对不上：

| 文件:行 | 原文 | 实际（逐行点 `^\| TC-0` 行） |
|---|---|---|
| `docs/task/task.1+环境准备.md:6` | TC-00「**18 个用例**，2026-09-13 生成」 | **27 条** |
| `docs/核心任务.md:22` | 「TC-00（**18 用例**）与 TC-01（**21 用例**）均已生成」 | TC-00 **27** 条；TC-01 **24** 条（25 行，其中 1 行是 `1.5-3 备注`） |

复核命令：

```bash
cd /mnt/c/wp/nexus-agent-workbench && grep -c '^| TC-00-' docs/test-cases/TC-00.md
cd /mnt/c/wp/nexus-agent-workbench && grep -c '^| TC-01-' docs/test-cases/TC-01.md
```

> ⚠️ **2026-09-18 复核**（数字已变，本条的**处置决定未变** —— 用户 2026-09-15 的「放入草稿、暂不订正」未被推翻，此处只更新事实）：
> 用上面同一条命令实测：**TC-00 = 30 行**、**TC-01 = 20 条用例（26 行，其中 6 行是 `备注` 行）**。
> TC-01 的变化来自同日**删除**五条用例：`1.3-3` / `1.3-4`（ut 与重复内容，见 TC-01 头部「编号对照」）、
> `1.4-4` / `1.4-5`（原步骤写业务表，撞 Test Harmlessness 红线且无接口可替代）、`1.4-6`（与 1.2-4 重复）
> —— 后三条见 TC-01 §3 表下的空号说明。
> ⇒ 现状：`docs/核心任务.md` 那句「TC-00（**18 用例**）与 TC-01（**21 用例**）」：TC-00 差 12，TC-01 差 1。
> 中途还出过一次**巧合** —— TC-01 删到 21 条时那句话恰好对了，随即又删一条就不对了。
> 这恰好说明下面的建议方向 (b)：**数字对上了不等于对**，它可能只是恰好漂到真值上，下一次改动又会偏。

## 影响

不影响任何判据（用例本身是好的），但会**误导验收工作量** —— 按「18 条」估进度，实际要跑 27 条。另外它与 TC-00 内部自述的口径也不一致（TC-00 的台账已按 27 条记）。

## 建议方向（未拍板）

二选一：
- **(a) 订正成实际数**：改为 TC-00 27 条 / TC-01 24 条。
- **(b) 去掉数字**：改成「见 `docs/test-cases/TC-XX.md` 内实际条目」—— 用例会随功能演进增删，写死的数字注定再次过期（本次就是这么来的）。

倾向 (b)：这类「派生事实」不该在文档里各存一份，与 `CLAUDE.md`「禁止重复维护」同一条思路。
