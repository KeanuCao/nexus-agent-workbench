# [AI发现] 发版脚本 `scripts/release.sh` 待定

- **发现时间**：2026-09-15 ｜ **发现场景**：回答「版本号每次要改多个地方，对外发布不方便」时提出 ｜ **状态**：待定（用户当日明确本次不做）

## 背景

2026-09-15 的版本号单点化改造（`${revision}` + flatten-maven-plugin）解决的是**「改一处、六处生效」**——
把父 POM 与 5 个子模块的版本号收敛成 `backend/pom.xml` 里的一行 `<revision>`。

但它解决不了**发版流程本身**：改完版本号之后，仍然要人去做「构建校验 → 提交 → 打 tag → 推送」这一串。

## 建议形态（待评估）

`scripts/release.sh <x.y.z>`：① 改 `backend/pom.xml` 的 `<revision>`；② 校验 6 个模块版本一致；
③ 提交；④ 打 `v<x.y.z>` 标签；⑤ **停下来，等用户手工 push + 合并**（push 与 PR merge 由用户手工执行，脚本不得代做）。

## 为什么现在不做

用户 2026-09-15 决定本次只做 POM 单点化 + 文档去硬编码，脚本挂起。

## 关联

- 它是「前端 `package.json` 版本对齐」的落点 —— 两处一起改才有意义，单独对齐反而让发版多一处要同步（见 `[AI发现]前端版本号未对齐.md`）。
- 它是「文档里的版本号示例」自动化同步的落点（本次改为人工口径：断言跟随 `<revision>`、示例标注非真源）。
